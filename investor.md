# FULLINVEST v5.0 - The Context-Aware AI Due Diligence Platform

> **La DD d'un fonds VC, accessible a un Business Angel solo.**
> Pas d'equipe ? Pas de probleme. L'IA fait le travail d'un analyste senior.
> Multi-model. Context-first. Value-first.

---

# EXECUTIVE SUMMARY

## La Cible : Business Angels (95%)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         POURQUOI LES BUSINESS ANGELS ?                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   LE PROBLEME DES BA AUJOURD'HUI                                                │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                                                 │
│                                                                                  │
│   • Pas d'equipe - Tu es seul face au deal                                      │
│   • Pas le temps - Tu as un autre job (souvent)                                 │
│   • Pas d'acces aux donnees - PitchBook coute 20K€/an                          │
│   • Pas de benchmarks - Tu ne sais pas si c'est cher ou pas                    │
│   • Pas de process - Chaque DD est reinventee from scratch                     │
│   • Information asymetrique - Le fondateur en sait plus que toi                │
│                                                                                  │
│   RESULTAT:                                                                     │
│   → 70% des BA investissent "au feeling"                                       │
│   → Red flags rates par manque de temps                                        │
│   → Pas de negociation (pas d'arguments)                                       │
│   → Mauvaises surprises post-investissement                                    │
│                                                                                  │
│   ─────────────────────────────────────────────────────────────────────────    │
│                                                                                  │
│   CE QUE FULLINVEST LEUR APPORTE                                               │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                                                │
│                                                                                  │
│   ┌─────────────────────────────────────────────────────────────────────┐      │
│   │                                                                      │      │
│   │   "Upload ton deck, en 5 minutes tu as:                             │      │
│   │                                                                      │      │
│   │    ✅ La DD qu'un analyste VC ferait en 2 jours                     │      │
│   │    ✅ Le contexte marche (50+ deals comparables)                    │      │
│   │    ✅ Les red flags detectes automatiquement                        │      │
│   │    ✅ Les questions a poser au fondateur                            │      │
│   │    ✅ Les arguments de negociation                                  │      │
│   │    ✅ Un score objectif pour decider"                               │      │
│   │                                                                      │      │
│   └─────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
│   QUI PEUT LE PLUS PEUT LE MOINS                                                │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━                                                  │
│                                                                                  │
│   Si on construit pour le BA solo (le cas le plus exigeant),                   │
│   alors les fonds VC, family offices, et autres pourront aussi l'utiliser.     │
│                                                                                  │
│   Cible primaire: Business Angels (95%)                                         │
│   Cible secondaire: Petits fonds, family offices, syndics (5%)                 │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## Le Persona Type

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  MARIE, 45 ans - Business Angel                                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Background:                                                                    │
│  • Ex-directrice marketing dans une scale-up (exit 50M€)                       │
│  • Investit depuis 3 ans, 8 startups au portfolio                              │
│  • Ticket moyen: 25K€                                                           │
│  • Fait partie de 2 reseaux BA                                                 │
│                                                                                  │
│  Son probleme:                                                                  │
│  • Recoit 10+ decks par mois via ses reseaux                                   │
│  • A max 2-3h par semaine pour regarder les deals                              │
│  • Ne sait jamais si une valo est "dans le marche" ou pas                      │
│  • A deja eu 2 mauvaises surprises (fondateur avec historique cache)           │
│  • Se sent en position de faiblesse face aux fondateurs tech                   │
│                                                                                  │
│  Ce qu'elle veut:                                                               │
│  • "Je veux savoir en 5 min si ca vaut le coup de creuser"                     │
│  • "Je veux des arguments solides pour negocier"                               │
│  • "Je veux etre sure de ne pas rater un red flag evident"                     │
│  • "Je veux avoir l'air pro face au fondateur"                                 │
│                                                                                  │
│  Ce qu'elle est prete a payer:                                                  │
│  • 50-100€/mois (cout d'un abonnement pro)                                     │
│  • Ou 20-50€/deal (pay-per-use)                                                │
│  • ROI evident: eviter UNE mauvaise decision = 25K€ sauves                     │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## Ce qu'on construit

Une plateforme de Due Diligence IA **pour Business Angels** qui :

1. **Fait le travail d'un analyste** - En 5 min, pas en 2 jours. Tu es seul mais tu as la puissance d'une equipe.

2. **Donne acces aux donnees pro** - 50K+ deals, benchmarks, comparables. Les memes donnees que les VCs, sans payer 20K€/an.

3. **Detecte les red flags** - Le fondateur a une startup ratee qu'il cache ? On le trouve. CV gonfle ? On le detecte.

4. **Prepare la negociation** - "La valo est 30% au-dessus du marche, voici les arguments pour negocier."

5. **Donne confiance** - Plus de "je sais pas trop, ca a l'air bien". Des decisions basees sur des donnees.

## BA vs VC : Pourquoi c'est different

| | Business Angel (notre cible) | Fonds VC |
|---|---|---|
| **Equipe** | Solo | Analystes, associates |
| **Temps pour DD** | 2-3h max | Plusieurs jours |
| **Acces donnees** | Zero | PitchBook, Dealroom (20K€+/an) |
| **Process** | Informel | Structure |
| **Besoin de Fullinvest** | CRITIQUE | Nice-to-have |

## Pourquoi c'est different

| BA sans Fullinvest | BA avec Fullinvest |
|-------------------|----------------|
| "Ca a l'air bien, j'investis" | "Score 78/100, top 15% des deals similaires" |
| Aucune idee si la valo est bonne | "85e percentile, 30% au-dessus du median" |
| Pas le temps de checker le fondateur | Red flags detectes en 30 secondes |
| Negociation au doigt mouille | Arguments chiffres, comparables a l'appui |
| Feeling | Data |

## LA VALEUR IMMEDIATE - Pas de "Cold Start"

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         CE QUE L'UTILISATEUR VOIT                               │
│                         DES SON PREMIER DEAL                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  L'utilisateur upload un deck...                                                │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                                                                          │    │
│  │   "Votre deal a ete compare a 47 levees similaires (SaaS B2B,           │    │
│  │    Seed, Europe, 12 derniers mois)"                                      │    │
│  │                                                                          │    │
│  │   ┌─────────────────────────────────────────────────────────────────┐   │    │
│  │   │  VALORISATION: 15M€ (30x ARR)                                   │   │    │
│  │   │                                                                  │   │    │
│  │   │  Position vs marche:     [=========|====] 85e percentile        │   │    │
│  │   │  Median secteur:         18x ARR                                 │   │    │
│  │   │  Range P25-P75:          12x - 24x                               │   │    │
│  │   │                                                                  │   │    │
│  │   │  ⚠️  4 deals a 25x+ ont fait down round depuis                  │   │    │
│  │   │  📉 Tendance Q4: -15% vs Q3 (cooling market)                    │   │    │
│  │   │                                                                  │   │    │
│  │   │  → VERDICT: Valorisation agressive                              │   │    │
│  │   │  → Fair value estimee: 10-13M€                                  │   │    │
│  │   └─────────────────────────────────────────────────────────────────┘   │    │
│  │                                                                          │    │
│  │   ┌─────────────────────────────────────────────────────────────────┐   │    │
│  │   │  FONDATEUR: Pierre Dupont                                        │   │    │
│  │   │                                                                  │   │    │
│  │   │  ✅ Exit precedent: SaaS vendu 12M€ en 2019 (TechCorp)          │   │    │
│  │   │  ✅ Background verifie: Ex-Google (2014-2018, confirmé)          │   │    │
│  │   │  ⚠️  Startup precedente: levee 3M€, fermee en 2021              │   │    │
│  │   │  🔗 Network: Connecte a 3 VCs qui ont investi dans le secteur   │   │    │
│  │   │                                                                  │   │    │
│  │   │  → Track record: POSITIF avec reserve                           │   │    │
│  │   └─────────────────────────────────────────────────────────────────┘   │    │
│  │                                                                          │    │
│  │   ┌─────────────────────────────────────────────────────────────────┐   │    │
│  │   │  PAYSAGE CONCURRENTIEL                                           │   │    │
│  │   │                                                                  │   │    │
│  │   │  🏢 Leader: CompetitorX (85M€ leves, Series B)                  │   │    │
│  │   │  📰 Activite recente:                                           │   │    │
│  │   │     - CompetitorY: Series A 12M€ (il y a 2 mois)                │   │    │
│  │   │     - CompetitorZ: Acquis par BigCorp (il y a 6 mois)           │   │    │
│  │   │  ⚠️  Google a depose 3 brevets adjacents en 2024                │   │    │
│  │   │                                                                  │   │    │
│  │   │  → Big Tech risk: MEDIUM                                        │   │    │
│  │   │  → Consolidation en cours dans le secteur                       │   │    │
│  │   └─────────────────────────────────────────────────────────────────┘   │    │
│  │                                                                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│  TOUT CA, DES LE PREMIER DEAL.                                                  │
│  PAS DE "UTILISEZ LA PLATEFORME POUR QU'ELLE APPRENNE".                         │
│  L'INTELLIGENCE EST DEJA LA.                                                    │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### D'où vient cette intelligence ?

| Source | Contenu | Mise à jour |
|--------|---------|-------------|
| **Crunchbase** | 50K+ deals, fondateurs, investisseurs | Quotidienne |
| **Dealroom** | Deals europeens, valuations, tendances | Quotidienne |
| **PitchBook** | Financials detailles, exits, comparables | Quotidienne |
| **Statista/Gartner** | TAM, benchmarks secteur, tendances marche | Hebdomadaire |
| **LinkedIn API** | Background fondateurs, employees, croissance | Quotidienne |
| **News APIs** | Actualites, annonces, controverses | Temps reel |
| **OpenView/Bessemer** | Benchmarks SaaS (NRR, CAC, etc.) | Trimestrielle |

**L'utilisateur n'a rien à faire.** Les données sont là. Les benchmarks sont calculés. L'intelligence est prête.

---

## BUSINESS MODEL - FREE vs PRO

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         MODELE FREEMIUM                                         │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                                                                          │   │
│   │   FREE (0EUR)                                                            │   │
│   │   ━━━━━━━━━━━                                                            │   │
│   │                                                                          │   │
│   │   • 5 analyses de deals par mois                                        │   │
│   │   • Tier 1 uniquement (screening rapide)                                │   │
│   │   • Extraction docs + GO/NO-GO + premiers red flags                     │   │
│   │   • AI Board: Teaser uniquement ("Voici ce que le Board analyserait")   │   │
│   │                                                                          │   │
│   │   → Objectif: Montrer la valeur, creer l'envie                          │   │
│   │   → Cout plateforme: ~$0.50/analyse (modeles rapides)                   │   │
│   │                                                                          │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                                                                          │   │
│   │   PRO (249EUR/mois)                                                      │   │
│   │   ━━━━━━━━━━━━━━━━━                                                      │   │
│   │                                                                          │   │
│   │   • Analyses illimitees                                                  │   │
│   │   • Tiers 1, 2 et 3 complets                                            │   │
│   │     - Tier 1: Screening (30 sec)                                        │   │
│   │     - Tier 2: Analyse approfondie par 13 agents (2-3 min)               │   │
│   │     - Tier 3: Synthese par 5 experts + 9 experts sectoriels             │   │
│   │   • 5 AI Board inclus par mois                                          │   │
│   │     - 4 LLMs TOP deliberent sur le deal                                 │   │
│   │     - Claude Opus 4.5, GPT-4 Turbo, Gemini Ultra, Mistral Large         │   │
│   │   • Boards supplementaires: 79EUR/board                                 │   │
│   │                                                                          │   │
│   │   → Objectif: Experience complete, valeur maximale                      │   │
│   │   → Marge: ~63% apres couts LLM                                         │   │
│   │                                                                          │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### Pourquoi ce modele ?

| Decision | Justification |
|----------|---------------|
| **5 deals FREE** | Assez pour essayer, pas assez pour rester indefiniment |
| **Tier 1 seul FREE** | Valeur visible mais incomplete - pousse vers PRO |
| **249EUR/mois** | Premium justifie: 1 erreur evitee = 25K+ sauves |
| **5 boards inclus** | BA voit ~10-15 deals/mois, utilise board sur les 3-5 ambigus |
| **79EUR/board extra** | 3 boards a 79 = 237EUR → autant prendre PRO (249EUR) |

### Comparaison marche

| Service | Prix | Ce qu'il offre |
|---------|------|----------------|
| 1h avocat | 200-500EUR | Conseils legaux ponctuels |
| 1 jour consultant DD | 800-1500EUR | Analyse manuelle d'un deal |
| Analyste junior/mois | 3-4K EUR | Ressource humaine temps partiel |
| PitchBook/mois | ~1700EUR | Donnees marche uniquement |
| **Fullinvest PRO** | **249EUR** | DD complete + AI Board + illimite |

### Implementation technique

| Fichier | Role |
|---------|------|
| `src/services/deal-limits/index.ts` | Gestion des limites FREE/PRO |
| `src/services/board-credits/index.ts` | Gestion des credits AI Board |
| `prisma/schema.prisma` (UserDealUsage) | Tracking usage mensuel |
| `src/app/api/analyze/route.ts` | Verification des permissions avant analyse |
| `src/app/(dashboard)/pricing/page.tsx` | Page pricing publique |

---

# KILLER FEATURES

> **Ce qui fait que les utilisateurs ne peuvent plus s'en passer.**
> Pas des gadgets. Des outils qui changent fondamentalement la façon de faire de la DD.

## Vue d'ensemble

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              KILLER FEATURES MAP                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   CORE FEATURES (Jour 1)                                                        │
│   ━━━━━━━━━━━━━━━━━━━━━━                                                        │
│                                                                                  │
│   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐              │
│   │   DEAL      │ │  RED FLAGS  │ │    ROI      │ │  QUESTIONS  │              │
│   │  SCORING    │ │    AUTO     │ │ SIMULATOR   │ │ STRATEGIQUES│              │
│   │             │ │             │ │             │ │             │              │
│   │ Multi-dim   │ │ Detection   │ │ Exit paths  │ │ DD checklist│              │
│   │ + Global    │ │ proactive   │ │ Dilution    │ │ Deal-specific│             │
│   │ + Comparatif│ │ Zero faux+  │ │ Comparables │ │ Interview   │              │
│   └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘              │
│                                                                                  │
│                         ┌─────────────────────┐                                 │
│                         │  CHALLENGE PARTNER  │                                 │
│                         │                     │                                 │
│                         │  Assumption checker │                                 │
│                         │  Blind spot finder  │                                 │
│                         │  Scenario explorer  │                                 │
│                         └─────────────────────┘                                 │
│                                                                                  │
│   MOAT FEATURE                                                                  │
│   ━━━━━━━━━━━━                                                                  │
│                                                                                  │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                      TRACK RECORD VISIBLE                                │   │
│   │                                                                          │   │
│   │   "Nos predictions passees vs outcomes reels - Transparence totale"     │   │
│   │                                                                          │   │
│   │   • Prediction 2023: "Red flag team" → Outcome: Pivot force 2024 ✓     │   │
│   │   • Prediction 2023: "Market timing bon" → Outcome: Series B x3 ✓      │   │
│   │   • Precision globale: 73% sur 847 deals analyses                       │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│   MOONSHOT FEATURES (Roadmap)                                                   │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━                                                    │
│                                                                                  │
│   ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐      │
│   │ Deal Sourcing │ │   Founder     │ │ Market Timing │ │  Portfolio    │      │
│   │   Proactif    │ │   Matching    │ │    Oracle     │ │  Synergies    │      │
│   └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘      │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## FEATURE 1 : Deal Scoring System

> **"Un score qui a du sens, pas une probabilite qui te ridiculise si tu te plantes."**

### Philosophie

```
❌ MAUVAIS                              ✅ BON
━━━━━━━━━                              ━━━━━━

"65% de chance de                      "Score global: 78/100
reussir"
                                       Dimensions:
→ Si ca rate, tu passes               • Team: 85/100 (serial founder, exit precedent)
  pour un idiot                        • Market: 72/100 (TAM solide, timing moyen)
→ Fausse precision                     • Product: 68/100 (early traction, tech risk)
→ Non actionnable                      • Timing: 90/100 (marche en croissance)
                                       • Competition: 65/100 (leader etabli)

                                       Position: Top 15% des deals Seed SaaS Europe

                                       Comparable: Similaire a [DealX] qui a fait x4"
```

### Architecture du Scoring

```typescript
interface DealScoringSystem {

  // ═══════════════════════════════════════════════════════════════════════════
  // DIMENSIONS DE SCORING
  // ═══════════════════════════════════════════════════════════════════════════

  dimensions: {

    // 1. TEAM SCORE
    team: {
      weight: 0.25;  // 25% du score global

      components: {
        founder_track_record: {
          weight: 0.35;
          scoring: {
            'successful_exit': 95,
            'acquired_acquihire': 75,
            'raised_series_b+': 70,
            'raised_series_a': 60,
            'first_time_founder_strong_background': 50,
            'first_time_founder_weak_background': 30,
            'red_flags_found': -30  // Malus
          };
        };

        team_completeness: {
          weight: 0.20;
          scoring: {
            'full_c_suite': 90,
            'ceo_cto_only': 70,
            'solo_founder_technical': 50,
            'solo_founder_non_technical': 30,
            'missing_critical_role': -20
          };
        };

        team_experience_relevance: {
          weight: 0.25;
          factors: ['domain_expertise', 'years_in_industry', 'previous_startup_experience'];
        };

        team_cohesion: {
          weight: 0.20;
          factors: ['worked_together_before', 'duration_of_collaboration', 'complementary_skills'];
        };
      };

      output: {
        score: number;           // 0-100
        grade: 'A' | 'B' | 'C' | 'D' | 'F';
        strengths: string[];
        concerns: string[];
        comparison: string;      // "Top 20% des teams Seed"
      };
    };

    // 2. MARKET SCORE
    market: {
      weight: 0.20;

      components: {
        tam_validity: {
          weight: 0.25;
          scoring: {
            'verified_large_growing': 90,
            'verified_large_stable': 70,
            'unverified_claimed_large': 40,
            'small_or_shrinking': 20
          };
        };

        timing: {
          weight: 0.30;
          factors: ['market_growth_rate', 'adoption_curve_position', 'regulatory_tailwinds'];
        };

        competitive_dynamics: {
          weight: 0.25;
          scoring: {
            'blue_ocean': 85,
            'fragmented_winnable': 75,
            'competitive_differentiated': 60,
            'crowded_commoditized': 30,
            'winner_take_all_late': 15
          };
        };

        macro_alignment: {
          weight: 0.20;
          factors: ['vc_appetite_for_sector', 'public_market_comps', 'recent_exits'];
        };
      };
    };

    // 3. PRODUCT SCORE
    product: {
      weight: 0.20;

      components: {
        traction: {
          weight: 0.35;
          factors: ['arr', 'growth_rate', 'user_count', 'engagement_metrics'];
          comparison_to_benchmarks: true;  // vs percentiles du stage
        };

        product_market_fit_signals: {
          weight: 0.30;
          factors: ['nps', 'retention', 'organic_growth', 'customer_quotes'];
        };

        technical_moat: {
          weight: 0.20;
          scoring: {
            'deep_tech_defensible': 90,
            'data_moat': 80,
            'network_effects': 85,
            'switching_costs': 70,
            'speed_execution': 50,
            'no_moat': 20
          };
        };

        scalability: {
          weight: 0.15;
          factors: ['unit_economics', 'gross_margin', 'cac_ltv_ratio'];
        };
      };
    };

    // 4. TIMING SCORE
    timing: {
      weight: 0.15;

      components: {
        market_cycle: {
          weight: 0.40;
          factors: ['sector_funding_trend', 'valuation_trend', 'exit_activity'];
        };

        technology_readiness: {
          weight: 0.30;
          factors: ['enabling_tech_maturity', 'infrastructure_availability'];
        };

        regulatory_environment: {
          weight: 0.30;
          factors: ['regulatory_clarity', 'upcoming_changes', 'compliance_burden'];
        };
      };
    };

    // 5. FINANCIAL SCORE
    financials: {
      weight: 0.20;

      components: {
        valuation_reasonableness: {
          weight: 0.40;
          method: 'compare_to_similar_deals';
          output: {
            percentile: number;      // "85e percentile = cher"
            vs_median: string;       // "+67% vs median"
            verdict: 'cheap' | 'fair' | 'expensive' | 'very_expensive';
          };
        };

        capital_efficiency: {
          weight: 0.25;
          factors: ['burn_multiple', 'arr_per_employee', 'runway'];
        };

        round_structure: {
          weight: 0.20;
          factors: ['dilution', 'liquidation_prefs', 'governance'];
        };

        path_to_next_round: {
          weight: 0.15;
          factors: ['milestones_achievable', 'runway_vs_milestones', 'follow_on_appetite'];
        };
      };
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // SCORE GLOBAL & POSITIONING
  // ═══════════════════════════════════════════════════════════════════════════

  globalScore: {

    calculate: (dimensionScores: DimensionScores) => {
      // Weighted average
      const weighted = Object.entries(dimensionScores).reduce((sum, [dim, score]) => {
        return sum + (score.score * dimensions[dim].weight);
      }, 0);

      return {
        score: Math.round(weighted),
        grade: scoreToGrade(weighted),

        // Breakdown
        dimensions: dimensionScores,

        // Strengths & concerns (top 3 each)
        top_strengths: extractTopStrengths(dimensionScores, 3),
        top_concerns: extractTopConcerns(dimensionScores, 3),

        // The money insight
        one_liner: generateOneLiner(dimensionScores)
        // Ex: "Strong team, fair valuation, but market timing is concerning"
      };
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // POSITIONNEMENT COMPARATIF
  // ═══════════════════════════════════════════════════════════════════════════

  comparativePosition: {

    calculate: async (deal: Deal, globalScore: number) => {
      // Trouver les deals comparables
      const comparables = await findComparableDeals({
        sector: deal.sector,
        stage: deal.stage,
        geography: deal.geography,
        time_window: '24_months'
      });

      // Calculer le percentile
      const percentile = calculatePercentile(globalScore, comparables.map(d => d.score));

      // Trouver les deals les plus similaires
      const mostSimilar = comparables
        .sort((a, b) => Math.abs(a.score - globalScore) - Math.abs(b.score - globalScore))
        .slice(0, 3);

      return {
        percentile: percentile,
        position_text: `Top ${100 - percentile}% des ${comparables.length} deals ${deal.stage} ${deal.sector}`,

        // Comparables proches
        similar_deals: mostSimilar.map(d => ({
          name: d.anonymized_name,  // "SaaS B2B #127"
          score: d.score,
          outcome: d.outcome,       // "Series A x2.5" ou "Echec"
          key_similarity: d.similarity_reason
        })),

        // Insight
        insight: generateComparativeInsight(deal, comparables, percentile)
        // Ex: "Les 5 deals avec un score similaire (75-80) ont leve Series A dans 80% des cas"
      };
    };
  };
}
```

### Output Example

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              DEAL SCORE: 78/100                                  │
│                                   Grade: B+                                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   DIMENSIONS                                                                    │
│   ──────────                                                                    │
│                                                                                  │
│   Team        [████████████████████░░░░] 85/100  ← Exit precedent, equipe complete│
│   Market      [████████████████░░░░░░░░] 72/100  ← TAM solide, timing moyen       │
│   Product     [███████████████░░░░░░░░░] 68/100  ← Traction early, PMF a prouver  │
│   Timing      [████████████████████████] 90/100  ← Secteur en croissance          │
│   Financials  [██████████████░░░░░░░░░░] 65/100  ← Valorisation 85e percentile    │
│                                                                                  │
│   POSITION                                                                       │
│   ────────                                                                       │
│                                                                                  │
│   📊 Top 15% des 127 deals Seed SaaS B2B Europe (24 derniers mois)              │
│                                                                                  │
│   DEALS SIMILAIRES                                                              │
│   ────────────────                                                              │
│                                                                                  │
│   • SaaS #089 (Score: 76) → Series A (x2.1), 14 mois apres                      │
│   • SaaS #112 (Score: 79) → Acquis (x1.8), 18 mois apres                        │
│   • SaaS #043 (Score: 77) → En cours, Series A en negociation                   │
│                                                                                  │
│   SYNTHESE                                                                       │
│   ────────                                                                       │
│                                                                                  │
│   ✅ Team solide avec track record prouve                                       │
│   ✅ Timing favorable, secteur dynamique                                        │
│   ⚠️  Valorisation agressive vs comparables                                     │
│   ⚠️  PMF a confirmer (metriques de retention a surveiller)                     │
│                                                                                  │
│   💡 "Profil de deal Series A probable, mais valorisation a negocier"           │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## FEATURE 2 : Red Flags Automatiques

> **"Detection proactive. Zero faux positifs. Confiance totale."**

### Philosophie

```
Le probleme des red flags:
- Trop de faux positifs → L'utilisateur arrete de les regarder
- Pas assez → On rate des deals toxiques

Notre approche:
- Chaque red flag a un NIVEAU DE CONFIANCE
- On ne montre que les red flags VERIFIES ou FORTEMENT SUSPECTES
- Chaque flag vient avec la SOURCE et la METHODE de detection
```

### Categories de Red Flags

```typescript
interface RedFlagSystem {

  // ═══════════════════════════════════════════════════════════════════════════
  // CATEGORIES DE RED FLAGS
  // ═══════════════════════════════════════════════════════════════════════════

  categories: {

    // 1. FOUNDER RED FLAGS
    founder: {
      flags: [
        {
          id: 'previous_failure_hidden',
          name: 'Echec precedent non mentionne',
          severity: 'high',
          detection: {
            method: 'cross_reference_crunchbase_linkedin_news',
            signals: ['company_not_in_deck', 'dates_gap', 'news_about_failure'],
          },
          example: "Fondateur a ete CEO de TechX (2019-2021), non mentionne. TechX a ferme apres avoir leve 5M€.",
        },
        {
          id: 'cv_discrepancy',
          name: 'Incoherence dans le CV',
          severity: 'critical',
          detection: {
            method: 'verify_claims_against_sources',
            signals: ['dates_mismatch', 'title_inflation', 'company_not_found'],
          },
          example: "Claim: 'VP Engineering chez Google 2015-2018'. Trouve: 'Senior Engineer 2016-2017'",
        },
        {
          id: 'legal_issues',
          name: 'Problemes legaux',
          severity: 'critical',
          detection: {
            method: 'court_records_news_search',
            signals: ['lawsuit', 'bankruptcy', 'regulatory_sanction'],
          },
        },
        {
          id: 'reputation_concerns',
          name: 'Reputation problematique',
          severity: 'medium',
          detection: {
            method: 'glassdoor_news_social_analysis',
            signals: ['negative_glassdoor_pattern', 'controversy', 'employee_complaints'],
          },
        },
        {
          id: 'cofounder_departure',
          name: 'Depart recent de co-fondateur',
          severity: 'high',
          detection: {
            method: 'linkedin_monitoring_news',
            signals: ['cofounder_left_recently', 'no_explanation_given'],
          },
        },
      ],
    },

    // 2. FINANCIAL RED FLAGS
    financial: {
      flags: [
        {
          id: 'metrics_inconsistency',
          name: 'Metriques incoherentes',
          severity: 'high',
          detection: {
            method: 'cross_validate_deck_numbers',
            signals: ['arr_vs_customers_mismatch', 'growth_math_wrong', 'dates_inconsistent'],
          },
          example: "ARR claim: 500K. 50 clients a 500€/mois = 300K. Ecart de 67%.",
        },
        {
          id: 'burn_rate_unsustainable',
          name: 'Burn rate critique',
          severity: 'high',
          detection: {
            method: 'calculate_runway_from_deck',
            signals: ['runway_under_12_months', 'burn_accelerating', 'no_path_to_profitability'],
          },
        },
        {
          id: 'valuation_outlier',
          name: 'Valorisation hors marche',
          severity: 'medium',
          detection: {
            method: 'compare_to_similar_deals',
            signals: ['above_95_percentile', 'no_justification', 'down_round_risk'],
          },
        },
        {
          id: 'cap_table_issues',
          name: 'Cap table problematique',
          severity: 'high',
          detection: {
            method: 'analyze_cap_table_if_available',
            signals: ['founders_under_50%', 'too_many_investors_early', 'complex_structure'],
          },
        },
      ],
    },

    // 3. MARKET RED FLAGS
    market: {
      flags: [
        {
          id: 'market_shrinking',
          name: 'Marche en contraction',
          severity: 'critical',
          detection: {
            method: 'verify_tam_with_external_sources',
            signals: ['negative_growth_rate', 'declining_funding', 'exits_drying_up'],
          },
        },
        {
          id: 'timing_too_late',
          name: 'Timing rate',
          severity: 'high',
          detection: {
            method: 'analyze_competitive_landscape',
            signals: ['winner_established', 'consolidation_happening', 'barriers_too_high'],
          },
        },
        {
          id: 'big_tech_threat',
          name: 'Menace Big Tech imminente',
          severity: 'critical',
          detection: {
            method: 'monitor_big_tech_signals',
            signals: ['google_product_announced', 'amazon_entering', 'microsoft_acquisition'],
          },
        },
        {
          id: 'regulatory_risk',
          name: 'Risque regulatoire',
          severity: 'medium_to_critical',
          detection: {
            method: 'monitor_regulatory_news',
            signals: ['new_regulation_coming', 'enforcement_increasing', 'business_model_at_risk'],
          },
        },
      ],
    },

    // 4. PRODUCT RED FLAGS
    product: {
      flags: [
        {
          id: 'no_real_traction',
          name: 'Pas de traction reelle',
          severity: 'high',
          detection: {
            method: 'verify_traction_claims',
            signals: ['only_pilots', 'no_paying_customers', 'engagement_metrics_missing'],
          },
        },
        {
          id: 'tech_risk_high',
          name: 'Risque technologique eleve',
          severity: 'medium',
          detection: {
            method: 'analyze_technical_claims',
            signals: ['unproven_technology', 'long_development_needed', 'dependencies_risky'],
          },
        },
        {
          id: 'no_moat',
          name: 'Pas de defensabilite',
          severity: 'medium',
          detection: {
            method: 'analyze_competitive_advantage',
            signals: ['easily_replicable', 'no_switching_costs', 'no_network_effects'],
          },
        },
      ],
    },

    // 5. DEAL STRUCTURE RED FLAGS
    deal_structure: {
      flags: [
        {
          id: 'unfavorable_terms',
          name: 'Termes defavorables',
          severity: 'medium',
          detection: {
            method: 'analyze_term_sheet',
            signals: ['high_liquidation_pref', 'full_ratchet', 'excessive_control'],
          },
        },
        {
          id: 'investor_red_flags',
          name: 'Investisseurs problematiques',
          severity: 'medium',
          detection: {
            method: 'research_existing_investors',
            signals: ['bad_reputation', 'conflicts_of_interest', 'no_follow_on_capacity'],
          },
        },
      ],
    },
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // PRESENTATION DES RED FLAGS
  // ═══════════════════════════════════════════════════════════════════════════

  presentation: {
    // Chaque red flag est presente avec:
    template: {
      severity: 'critical' | 'high' | 'medium' | 'low';
      confidence: number;  // 0-100%
      title: string;
      description: string;
      evidence: string[];  // Sources et preuves
      impact: string;      // Pourquoi c'est un probleme
      mitigation: string;  // Comment le gerer si on proceed
      questions_to_ask: string[];  // Questions pour le fondateur
    };
  };
}
```

### Output Example

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              RED FLAGS DETECTES                                  │
│                              2 Critical · 1 High · 1 Medium                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  🔴 CRITICAL - Confiance: 95%                                                   │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━                                                    │
│                                                                                  │
│  ECHEC PRECEDENT NON MENTIONNE                                                  │
│                                                                                  │
│  Le fondateur Pierre Dupont a ete CEO de DataFlow (2019-2021),                  │
│  non mentionne dans le deck.                                                    │
│                                                                                  │
│  📎 Evidence:                                                                   │
│     • LinkedIn: CEO DataFlow (Jan 2019 - Dec 2021)                              │
│     • TechCrunch (Mars 2022): "DataFlow shuts down after raising $5M"           │
│     • Crunchbase: DataFlow - Status: Closed                                     │
│                                                                                  │
│  ⚠️  Impact: Manque de transparence. Potentiel pattern d'echec.                 │
│                                                                                  │
│  🛡️  Mitigation: Demander une explication directe. Un echec n'est pas          │
│      eliminatoire, mais le cacher est un signal negatif.                        │
│                                                                                  │
│  ❓ Questions a poser:                                                          │
│     1. "Pouvez-vous nous parler de votre experience chez DataFlow?"             │
│     2. "Quelles lecons en avez-vous tire pour ce nouveau projet?"               │
│                                                                                  │
│  ─────────────────────────────────────────────────────────────────────────────  │
│                                                                                  │
│  🔴 CRITICAL - Confiance: 88%                                                   │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━                                                    │
│                                                                                  │
│  MARCHE EN CONTRACTION                                                          │
│                                                                                  │
│  Le marche cible (EdTech B2C) montre des signes de contraction.                 │
│                                                                                  │
│  📎 Evidence:                                                                   │
│     • Dealroom: Funding EdTech B2C Europe -45% YoY                              │
│     • 3 exits negatifs dans les 6 derniers mois (Learnify, EduPro, StudyMate)   │
│     • Gartner: "EdTech B2C entering consolidation phase"                        │
│                                                                                  │
│  ⚠️  Impact: Difficulte a lever des tours suivants. Exit multiples en baisse.   │
│                                                                                  │
│  ─────────────────────────────────────────────────────────────────────────────  │
│                                                                                  │
│  🟠 HIGH - Confiance: 82%                                                       │
│  ━━━━━━━━━━━━━━━━━━━━━━━━                                                       │
│                                                                                  │
│  VALORISATION 92e PERCENTILE                                                    │
│                                                                                  │
│  Valorisation demandee: 15M€ (30x ARR)                                          │
│  Median des deals comparables: 18x ARR                                          │
│                                                                                  │
│  📎 Evidence:                                                                   │
│     • 47 deals Seed SaaS B2B Europe (24 mois)                                   │
│     • Seulement 4 deals au-dessus de 25x, dont 2 ont fait down round            │
│                                                                                  │
│  ⚠️  Impact: Risque de down round. Barre haute pour Series A.                   │
│                                                                                  │
│  🛡️  Mitigation: Negocier 20-22x (12-13M€). Proposer des milestones.            │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## FEATURE 3 : ROI Simulator

> **"Modeliser tous les scenarios de sortie avec des donnees reelles."**

```typescript
interface ROISimulator {

  // ═══════════════════════════════════════════════════════════════════════════
  // EXIT SCENARIOS
  // ═══════════════════════════════════════════════════════════════════════════

  exitScenarios: {

    generate: async (deal: Deal, investment: Investment) => {

      // Recuperer les exits comparables
      const comparableExits = await getComparableExits({
        sector: deal.sector,
        entry_stage: deal.stage,
        geography: deal.geography,
        time_window: '5_years'
      });

      return {
        // Scenario 1: Acquisition early (acqui-hire to small exit)
        early_acquisition: {
          probability_bucket: 'likely',  // Pas de % precis
          timeline: '2-3 ans',
          exit_value_range: { low: 5_000_000, mid: 15_000_000, high: 30_000_000 },
          multiple_on_investment: { low: 0.5, mid: 1.5, high: 3.0 },
          typical_acquirers: ['Strategic players', 'PE roll-ups'],
          comparable_exits: comparableExits.filter(e => e.type === 'acquisition_small'),
        },

        // Scenario 2: Series B+ puis acquisition majeure
        growth_then_acquisition: {
          probability_bucket: 'possible',
          timeline: '4-6 ans',
          exit_value_range: { low: 50_000_000, mid: 150_000_000, high: 400_000_000 },
          multiple_on_investment: { low: 3, mid: 10, high: 25 },
          dilution_to_exit: '60-75%',  // Ta part apres dilution
          typical_acquirers: ['Big Tech', 'Large strategics', 'PE'],
          comparable_exits: comparableExits.filter(e => e.type === 'acquisition_large'),
        },

        // Scenario 3: IPO track
        ipo: {
          probability_bucket: 'unlikely_but_possible',
          timeline: '7-10 ans',
          exit_value_range: { low: 500_000_000, mid: 1_500_000_000, high: 5_000_000_000 },
          multiple_on_investment: { low: 20, mid: 50, high: 150 },
          dilution_to_exit: '80-90%',
          requirements: ['$100M+ ARR', 'Path to profitability', 'Market leadership'],
          comparable_exits: comparableExits.filter(e => e.type === 'ipo'),
        },

        // Scenario 4: Echec
        failure: {
          probability_bucket: 'possible',
          timeline: '2-4 ans',
          exit_value_range: { low: 0, mid: 0, high: 500_000 },
          multiple_on_investment: { low: 0, mid: 0, high: 0.1 },
          common_causes: ['Market timing', 'Competition', 'Execution', 'Funding gap'],
          comparable_failures: comparableExits.filter(e => e.type === 'failure'),
        },
      };
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // DILUTION PATH
  // ═══════════════════════════════════════════════════════════════════════════

  dilutionPath: {

    project: (deal: Deal, investment: Investment) => {

      // Hypotheses basees sur les rounds typiques
      const rounds = [
        { name: 'Seed (current)', amount: deal.raise_amount, dilution: deal.dilution },
        { name: 'Series A', amount: 8_000_000, dilution: 0.20, typical_timing: '+18 mois' },
        { name: 'Series B', amount: 25_000_000, dilution: 0.18, typical_timing: '+36 mois' },
        { name: 'Series C', amount: 60_000_000, dilution: 0.15, typical_timing: '+54 mois' },
      ];

      // Calculer l'ownership a chaque etape
      let yourOwnership = investment.amount / deal.post_money;

      const path = rounds.map(round => {
        const result = {
          round: round.name,
          your_ownership: yourOwnership,
          your_stake_value: yourOwnership * round.post_money,
        };

        yourOwnership = yourOwnership * (1 - round.dilution);
        return result;
      });

      return {
        path: path,
        final_ownership_range: { low: yourOwnership * 0.8, high: yourOwnership * 1.2 },
        insight: `Votre ${investment.amount}€ represente ${(investment.amount / deal.post_money * 100).toFixed(1)}% aujourd'hui,
                  probablement ${(yourOwnership * 100).toFixed(1)}% a l'exit apres dilution.`
      };
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // COMPARABLE EXITS (Real Data)
  // ═══════════════════════════════════════════════════════════════════════════

  comparableExits: {

    display: (exits: Exit[]) => {
      return exits.map(exit => ({
        company: exit.name,
        sector: exit.sector,
        entry_stage: exit.entry_stage,
        entry_valuation: exit.entry_valuation,
        exit_type: exit.exit_type,
        exit_value: exit.exit_value,
        time_to_exit: exit.time_to_exit,
        multiple: exit.exit_value / exit.entry_valuation,
        acquirer: exit.acquirer,

        // What made it work/fail
        key_factors: exit.key_factors,

        // Similarity to current deal
        similarity_score: exit.similarity_score,
        similarity_reasons: exit.similarity_reasons,
      }));
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // MONTE CARLO DISTRIBUTION
  // ═══════════════════════════════════════════════════════════════════════════

  monteCarlo: {

    simulate: (deal: Deal, investment: Investment, simulations: number = 10000) => {
      // Distribution basee sur les outcomes historiques de deals similaires

      const results = [];
      const historicalOutcomes = getHistoricalOutcomeDistribution(deal.sector, deal.stage);

      for (let i = 0; i < simulations; i++) {
        const outcome = sampleFromDistribution(historicalOutcomes);
        const dilution = sampleDilutionPath(deal.stage, outcome.exit_stage);
        const exitMultiple = sampleExitMultiple(outcome.exit_type, deal.sector);

        const finalValue = investment.amount * exitMultiple * (1 - dilution);
        results.push(finalValue);
      }

      return {
        distribution: computeDistribution(results),
        percentiles: {
          p10: percentile(results, 10),
          p25: percentile(results, 25),
          p50: percentile(results, 50),
          p75: percentile(results, 75),
          p90: percentile(results, 90),
        },
        expected_value: mean(results),
        probability_of_loss: results.filter(r => r < investment.amount).length / simulations,
        probability_of_2x: results.filter(r => r > investment.amount * 2).length / simulations,
        probability_of_10x: results.filter(r => r > investment.amount * 10).length / simulations,
      };
    };
  };
}
```

### Output Example

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              ROI SIMULATOR                                       │
│                              Investment: 500K€ @ 15M€ post                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  SCENARIOS DE SORTIE                                                            │
│  ━━━━━━━━━━━━━━━━━━━                                                            │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │  ACQUISITION EARLY (2-3 ans)                                    LIKELY  │    │
│  │                                                                          │    │
│  │  Exit value: 5M€ - 15M€ - 30M€                                          │    │
│  │  Votre retour: 165K€ - 495K€ - 990K€                                    │    │
│  │  Multiple: 0.3x - 1x - 2x                                               │    │
│  │                                                                          │    │
│  │  Acquéreurs typiques: PE roll-up, Strategic player                      │    │
│  │                                                                          │    │
│  │  📊 3 exits similaires:                                                 │    │
│  │     • TechCo → Acquired 12M€ (1.2x en 2 ans)                           │    │
│  │     • DataApp → Acquired 8M€ (0.8x en 3 ans)                           │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │  GROWTH → ACQUISITION (4-6 ans)                               POSSIBLE  │    │
│  │                                                                          │    │
│  │  Exit value: 50M€ - 150M€ - 400M€                                       │    │
│  │  Dilution estimee: ~70%                                                 │    │
│  │  Votre retour: 500K€ - 1.5M€ - 4M€                                      │    │
│  │  Multiple: 1x - 3x - 8x                                                 │    │
│  │                                                                          │    │
│  │  📊 5 exits similaires:                                                 │    │
│  │     • SaaSCorp → Acquired 180M€ by Salesforce (12x seed, 5 ans)        │    │
│  │     • CloudTech → Acquired 95M€ by Microsoft (6x seed, 4 ans)          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│  DILUTION PATH                                                                  │
│  ━━━━━━━━━━━━━━                                                                 │
│                                                                                  │
│  Votre 500K€ = 3.33% aujourd'hui                                               │
│                                                                                  │
│  Seed (now)   [███████████████████████████████████] 3.33%                      │
│  Series A     [██████████████████████████░░░░░░░░░] 2.67%  (-20%)              │
│  Series B     [█████████████████████░░░░░░░░░░░░░░] 2.19%  (-18%)              │
│  Series C     [██████████████████░░░░░░░░░░░░░░░░░] 1.86%  (-15%)              │
│  Exit         [████████████████░░░░░░░░░░░░░░░░░░░] ~1.0%  (ESOP, etc.)        │
│                                                                                  │
│  DISTRIBUTION DES RETOURS (Monte Carlo, 10K simulations)                        │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                          │
│                                                                                  │
│                          ▂▃▅▆█▆▅▃▂                                              │
│                       ▁▂▃▄▅▆███████▆▅▄▃▂▁                                       │
│                    ▁▂▃▄▅▆████████████████▆▅▄▃▂▁                                 │
│  ──────────────────────────────────────────────────────────────                 │
│  0x        1x        3x        5x       10x       20x+                          │
│                                                                                  │
│  P10: 0.2x (100K€)    P50: 1.8x (900K€)    P90: 8x (4M€)                        │
│                                                                                  │
│  Probabilite de perte (< 1x): 35%                                              │
│  Probabilite de 3x+: 42%                                                        │
│  Probabilite de 10x+: 12%                                                       │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## FEATURE 4 : Questions Strategiques

> **"Les bonnes questions au bon moment. Preparation totale."**

```typescript
interface StrategicQuestionsSystem {

  // ═══════════════════════════════════════════════════════════════════════════
  // DD CHECKLIST (Standard)
  // ═══════════════════════════════════════════════════════════════════════════

  ddChecklist: {
    categories: {
      team: {
        questions: [
          { q: "Background des fondateurs verifie?", importance: 'critical' },
          { q: "References appelees?", importance: 'high' },
          { q: "Equity split et vesting en place?", importance: 'high' },
          { q: "Key man risk identifie?", importance: 'medium' },
          // ... 20+ questions
        ];
      },
      product: { /* ... */ },
      market: { /* ... */ },
      financials: { /* ... */ },
      legal: { /* ... */ },
      technology: { /* ... */ },
    };

    // Auto-track completion
    trackProgress: (dealId: string, checkedItems: string[]) => CompletionStatus;
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // DEAL-SPECIFIC QUESTIONS (Generated)
  // ═══════════════════════════════════════════════════════════════════════════

  dealSpecificQuestions: {

    generate: async (deal: Deal, redFlags: RedFlag[], context: Context) => {

      const questions = [];

      // Questions basees sur les red flags
      for (const flag of redFlags) {
        questions.push({
          category: 'red_flag_investigation',
          question: flag.questions_to_ask[0],
          why_important: flag.impact,
          what_to_look_for: flag.mitigation,
          red_flag_reference: flag.id,
        });
      }

      // Questions basees sur les gaps dans le deck
      const gaps = identifyInformationGaps(deal);
      for (const gap of gaps) {
        questions.push({
          category: 'information_gap',
          question: gap.question,
          why_important: gap.why_needed,
          what_good_answer_looks_like: gap.expected_answer,
        });
      }

      // Questions basees sur les comparables
      const comparableInsights = await getComparableInsights(deal);
      for (const insight of comparableInsights.learnings) {
        questions.push({
          category: 'comparable_learning',
          question: insight.question,
          context: insight.context,
          what_we_learned_from_others: insight.learning,
        });
      }

      // Questions sur les assumptions implicites
      const assumptions = identifyImplicitAssumptions(deal);
      for (const assumption of assumptions) {
        questions.push({
          category: 'assumption_validation',
          question: assumption.question,
          implicit_assumption: assumption.assumption,
          risk_if_wrong: assumption.risk,
        });
      }

      return questions;
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // FOUNDER INTERVIEW PREP
  // ═══════════════════════════════════════════════════════════════════════════

  founderInterviewPrep: {

    generate: async (deal: Deal, context: Context) => {

      return {
        // Icebreakers
        opening: [
          "Comment en etes-vous arrive a lancer [Company]?",
          "Qu'est-ce qui vous a fait quitter [Previous Job]?",
        ],

        // Questions cles par theme
        themes: {
          vision: {
            questions: [
              {
                q: "Ou voyez-vous l'entreprise dans 5 ans?",
                what_to_listen_for: ['Ambition realiste', 'Vision claire du marche', 'Etapes definies'],
                red_flags: ['Vague', 'Trop ambitieux sans plan', 'Pas de metrics'],
              },
              // ...
            ],
          },

          market: {
            questions: [
              {
                q: "Pourquoi maintenant? Qu'est-ce qui a change dans le marche?",
                what_to_listen_for: ['Insight unique', 'Timing defendable', 'Data points'],
                red_flags: ['Pas de reponse claire', 'Timing opportuniste'],
              },
              // ...
            ],
          },

          competition: {
            questions: [
              {
                q: "Qui est votre plus gros concurrent et pourquoi allez-vous gagner?",
                what_to_listen_for: ['Connaissance du paysage', 'Differentiation claire', 'Humilite'],
                red_flags: ['Nie la competition', 'Pas de differentiation', 'Arrogance'],
                context_from_analysis: context.competition.top_competitors,
              },
              // ...
            ],
          },

          team: {
            questions: [
              {
                q: "Parlez-moi de votre co-fondateur. Comment vous etes-vous rencontres?",
                what_to_listen_for: ['Histoire authentique', 'Complementarite', 'Respect mutuel'],
                red_flags: ['Tension visible', 'Roles pas clairs', 'Historique court'],
              },
              // ...
            ],
          },

          failures: {
            questions: [
              {
                q: "Qu'est-ce qui n'a pas marche jusqu'ici? Quelle a ete votre plus grosse erreur?",
                what_to_listen_for: ['Honnetete', 'Learning', 'Resilience'],
                red_flags: ['Blame les autres', 'Pas d\'erreurs admises', 'Pas de learning'],
              },
              // ...
            ],
          },
        },

        // Questions pièges (gentilles)
        probing: [
          {
            q: "Si vous n'aviez pas leve ce tour, que feriez-vous?",
            purpose: 'Tester le plan B et la resilience',
          },
          {
            q: "Quel est le scenario dans lequel vous echouez?",
            purpose: 'Tester la lucidite sur les risques',
          },
        ],

        // Closing
        closing: [
          "Qu'est-ce que vous voudriez que je sache qui n'est pas dans le deck?",
          "Quelles questions est-ce qu'on ne vous pose jamais mais qu'on devrait?",
        ],
      };
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // REFERENCE CHECK GUIDE
  // ═══════════════════════════════════════════════════════════════════════════

  referenceCheckGuide: {

    generate: (founders: Founder[], context: Context) => {

      return founders.map(founder => ({
        founder: founder.name,

        // Qui appeler
        suggested_references: [
          { type: 'ex-colleague', from: founder.previous_companies[0], priority: 'high' },
          { type: 'ex-manager', from: founder.previous_companies[0], priority: 'high' },
          { type: 'ex-investor', from: founder.previous_ventures, priority: 'medium' },
          { type: 'ex-employee', from: 'current_company', priority: 'medium' },
        ],

        // Questions a poser
        questions: [
          {
            q: "Comment decririez-vous [Founder] en 3 mots?",
            purpose: 'Character snapshot',
          },
          {
            q: "Quelle est sa plus grande force?",
            purpose: 'Identifier les superpouvoirs',
          },
          {
            q: "Sur quoi doit-il/elle travailler?",
            purpose: 'Identifier les faiblesses',
            what_to_listen_for: 'Hesitation, reponse vague = potentiel red flag',
          },
          {
            q: "Reinvestiriez-vous / retravailleriez-vous avec lui/elle?",
            purpose: 'Ultimate test',
            what_to_listen_for: 'Oui enthousiaste vs oui hesitant',
          },
          {
            q: "Y a-t-il quelque chose que je devrais savoir?",
            purpose: 'Ouvrir la porte aux red flags',
          },
        ],

        // Points specifiques a valider (basé sur l'analyse)
        specific_validations: context.red_flags
          .filter(f => f.category === 'founder' && f.founder === founder.name)
          .map(f => ({
            what_to_validate: f.title,
            suggested_question: f.questions_to_ask[0],
          })),
      }));
    };
  };
}
```

---

## FEATURE 5 : Challenge Partner

> **"Une IA qui ne dit pas oui a tout. Qui trouve ce que tu n'as pas vu."**

```typescript
interface ChallengePartner {

  // ═══════════════════════════════════════════════════════════════════════════
  // ASSUMPTION CHECKER
  // ═══════════════════════════════════════════════════════════════════════════

  assumptionChecker: {

    identify: async (deal: Deal) => {

      // Extraire les claims du deck
      const claims = extractClaimsFromDeck(deal.documents);

      // Identifier les assumptions implicites
      const assumptions = [];

      for (const claim of claims) {
        const implicitAssumptions = identifyImplicitAssumptions(claim);

        for (const assumption of implicitAssumptions) {
          assumptions.push({
            claim: claim.text,
            assumption: assumption.text,

            // Est-ce que c'est vrai?
            validation: {
              status: 'unverified' | 'partially_verified' | 'verified' | 'contradicted',
              evidence: string[],
              confidence: number,
            },

            // Qu'est-ce qui se passe si c'est faux?
            risk_if_wrong: {
              impact: 'critical' | 'high' | 'medium' | 'low',
              scenario: string,
            },

            // Comment verifier?
            how_to_verify: string[],
          });
        }
      }

      return assumptions;
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // BLIND SPOT FINDER
  // ═══════════════════════════════════════════════════════════════════════════

  blindSpotFinder: {

    find: async (deal: Deal, analysis: Analysis) => {

      const blindSpots = [];

      // 1. Ce qui n'est PAS dans le deck mais devrait y etre
      const missingTopics = identifyMissingTopics(deal.documents, deal.sector);
      for (const topic of missingTopics) {
        blindSpots.push({
          type: 'missing_from_deck',
          topic: topic.name,
          why_important: topic.importance,
          question: `Le deck ne mentionne pas ${topic.name}. Pourquoi?`,
        });
      }

      // 2. Questions que personne ne pose mais qu'on devrait
      const uncommonQuestions = await getUncommonButImportantQuestions(deal.sector);
      for (const q of uncommonQuestions) {
        blindSpots.push({
          type: 'rarely_asked',
          question: q.question,
          why_important: q.why,
          what_we_learned: q.learning_from_past_deals,
        });
      }

      // 3. Angles morts basés sur les échecs similaires
      const similarFailures = await getSimilarFailures(deal);
      for (const failure of similarFailures) {
        if (failure.warning_signs_visible_in_hindsight) {
          blindSpots.push({
            type: 'learned_from_failure',
            company: failure.name,
            what_happened: failure.failure_reason,
            warning_sign: failure.warning_signs_visible_in_hindsight,
            question: `[${failure.name}] a echoue parce que ${failure.failure_reason}.
                       Comment ce deal evite-t-il le meme piege?`,
          });
        }
      }

      // 4. Ce que le fondateur ne dit pas
      const unsaidThings = identifyWhatFounderIsNotSaying(deal);
      for (const thing of unsaidThings) {
        blindSpots.push({
          type: 'unsaid',
          observation: thing.observation,
          question: thing.question,
          why_suspicious: thing.reasoning,
        });
      }

      return blindSpots;
    };
  };

  // ═══════════════════════════════════════════════════════════════════════════
  // SCENARIO EXPLORER
  // ═══════════════════════════════════════════════════════════════════════════

  scenarioExplorer: {

    explore: async (deal: Deal) => {

      const scenarios = [];

      // Scenario 1: Market shifts
      scenarios.push({
        category: 'market',
        scenarios: [
          {
            name: 'Market decline',
            trigger: 'Le marche se contracte de 30% en 18 mois',
            probability: 'possible',
            impact_on_deal: await analyzeImpact(deal, 'market_decline_30'),
            questions: ['Quel est le plan si le marche se contracte?', 'Quels sont les couts fixes?'],
          },
          {
            name: 'Market acceleration',
            trigger: 'Le marche accelere, nouveaux entrants',
            probability: 'possible',
            impact_on_deal: await analyzeImpact(deal, 'market_acceleration'),
            questions: ['Comment scaler rapidement si le marche explose?', 'Risque de se faire depasser?'],
          },
        ],
      });

      // Scenario 2: Competition
      scenarios.push({
        category: 'competition',
        scenarios: [
          {
            name: 'Big Tech enters',
            trigger: 'Google/Amazon/Microsoft lance un produit concurrent',
            probability: deal.context.competition.big_tech_risk,
            impact_on_deal: 'Severe - differentiation critique',
            questions: ['Quel est le moat face a Big Tech?', 'Pivot possible?'],
          },
          {
            name: 'Leader acquires competitor',
            trigger: `${deal.context.competition.leader} acquiert un concurrent`,
            probability: 'possible',
            impact_on_deal: await analyzeImpact(deal, 'consolidation'),
            questions: ['Position dans un marche consolide?', 'Cible d\'acquisition?'],
          },
        ],
      });

      // Scenario 3: Team
      scenarios.push({
        category: 'team',
        scenarios: [
          {
            name: 'Founder departure',
            trigger: 'Le CEO/CTO quitte l\'entreprise',
            probability: 'unlikely_but_possible',
            impact_on_deal: 'Critical si key man risk',
            questions: ['Equity et vesting en place?', 'Succession plan?'],
          },
          {
            name: 'Key hire fails',
            trigger: 'Impossible de recruter les profils cles',
            probability: 'possible',
            impact_on_deal: await analyzeImpact(deal, 'hiring_failure'),
            questions: ['Plan de recrutement realiste?', 'Compensation competitive?'],
          },
        ],
      });

      // Scenario 4: Funding environment
      scenarios.push({
        category: 'funding',
        scenarios: [
          {
            name: 'Funding winter',
            trigger: 'Series A devient impossible a lever',
            probability: 'possible',
            impact_on_deal: 'Critical - runway et plan B',
            questions: ['Path to profitability?', 'Runway si pas de Series A?'],
          },
          {
            name: 'Down round',
            trigger: 'Series A a valorisation inferieure',
            probability: deal.context.valuation_percentile > 80 ? 'likely' : 'possible',
            impact_on_deal: 'Dilution supplementaire, moral team',
            questions: ['Valorisation defensive?', 'Milestones realistes?'],
          },
        ],
      });

      return scenarios;
    };
  };
}
```

### Output Example: Challenge Partner

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CHALLENGE PARTNER                                   │
│                              "Ce que tu n'as peut-etre pas vu"                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  🔍 ASSUMPTIONS NON VERIFIEES                                                   │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━                                                    │
│                                                                                  │
│  1. "Le marche vaut 5B€ et croit de 25%/an"                                     │
│     ⚠️  Assumption: Ce TAM est adressable par une startup                       │
│     🔴 Non verifie: Gartner dit 3.2B€, croissance 18%                           │
│     💥 Si faux: Le SOM realiste est peut-etre 10x plus petit                    │
│                                                                                  │
│  2. "Notre NRR est de 140%"                                                     │
│     ⚠️  Assumption: Mesure sur une cohorte representative                       │
│     🟡 Partiellement verifie: Seulement 6 mois de data                          │
│     💥 Si faux: Unit economics moins solides qu'annonces                        │
│                                                                                  │
│  ─────────────────────────────────────────────────────────────────────────────  │
│                                                                                  │
│  🎯 BLIND SPOTS                                                                  │
│  ━━━━━━━━━━━━━                                                                  │
│                                                                                  │
│  1. PAS DANS LE DECK: Churn rate                                                │
│     → "Le deck ne mentionne jamais le churn. Pour un SaaS, c'est etrange."      │
│     → Question: "Quel est votre churn mensuel? Pourquoi n'est-il pas dans le deck?"│
│                                                                                  │
│  2. QUESTION RAREMENT POSEE                                                     │
│     → "Combien de temps entre premier contact et signature client?"             │
│     → Pourquoi: Sales cycle long = cash burn eleve, previsibilite faible        │
│     → Learning: 3 deals similaires ont echoue a cause de sales cycles trop longs │
│                                                                                  │
│  3. APPRIS D'UN ECHEC SIMILAIRE                                                 │
│     → DataTech (2022) a leve 4M€ dans le meme secteur, ferme en 2024            │
│     → Cause: "Pas de differentiation face au leader qui a baisse ses prix"      │
│     → Question: "Comment reagissez-vous si [Leader] divise ses prix par 2?"     │
│                                                                                  │
│  ─────────────────────────────────────────────────────────────────────────────  │
│                                                                                  │
│  🌪️  SCENARIOS A EXPLORER                                                       │
│  ━━━━━━━━━━━━━━━━━━━━━                                                          │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │  ET SI... Google lance un produit concurrent?                           │    │
│  │                                                                          │    │
│  │  Probabilite: MEDIUM (brevets deposes, job postings detectes)           │    │
│  │  Impact: SEVERE                                                          │    │
│  │                                                                          │    │
│  │  Questions a poser:                                                      │    │
│  │  • "Quelle est votre defensabilite face a Google?"                      │    │
│  │  • "Pivot possible vers un segment moins expose?"                        │    │
│  │  • "Pourquoi un client choisirait vous vs Google gratuit?"              │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │  ET SI... Series A impossible a lever?                                  │    │
│  │                                                                          │    │
│  │  Probabilite: POSSIBLE (valorisation haute, marche froid)               │    │
│  │  Impact: CRITICAL                                                        │    │
│  │                                                                          │    │
│  │  Questions a poser:                                                      │    │
│  │  • "Quel est votre runway actuel?"                                      │    │
│  │  • "Path to profitability sans Series A?"                               │    │
│  │  • "Quels couts pouvez-vous couper en urgence?"                         │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## FEATURE 6 : Track Record Visible (MOAT)

> **"Transparence totale. On montre nos predictions passees vs la realite."**

```typescript
interface TrackRecordSystem {

  // Public dashboard showing our historical accuracy
  publicDashboard: {

    // Global metrics
    globalMetrics: {
      total_deals_analyzed: number;
      deals_with_known_outcomes: number;

      // Red flags accuracy
      red_flags: {
        total_raised: number;
        true_positives: number;      // Flag raised, problem occurred
        false_positives: number;     // Flag raised, no problem
        true_negatives: number;      // No flag, no problem
        false_negatives: number;     // No flag, problem occurred
        precision: number;           // TP / (TP + FP)
        recall: number;              // TP / (TP + FN)
      };

      // Score correlation
      scoring: {
        correlation_score_vs_outcome: number;  // 0-1
        top_quartile_success_rate: number;     // % of top 25% scores that succeeded
        bottom_quartile_failure_rate: number;  // % of bottom 25% scores that failed
      };
    };

    // Example predictions vs outcomes
    examples: Array<{
      deal_id: string;
      date: Date;

      // What we said
      our_prediction: {
        score: number;
        key_strengths: string[];
        key_concerns: string[];
        red_flags: string[];
      };

      // What happened
      outcome: {
        status: 'success' | 'moderate' | 'failure';
        details: string;
        timeline: string;
      };

      // Assessment
      accuracy: 'spot_on' | 'partially_correct' | 'missed';
    }>;
  };
}
```

---

## MOONSHOT FEATURES (Roadmap)

### 1. Deal Sourcing Proactif

```typescript
// L'IA trouve des deals qui matchent tes criteres AVANT qu'ils ne levent
interface DealSourcing {

  // Define your investment thesis
  thesis: {
    sectors: string[];
    stages: string[];
    geographies: string[];
    min_traction: TractionCriteria;
    founder_profile: FounderCriteria;
  };

  // Continuously scan for matches
  scanning: {
    sources: ['linkedin_job_postings', 'product_hunt', 'github_trending',
              'news_mentions', 'conference_speakers', 'accelerator_batches'];

    signals: ['hiring_spike', 'product_launch', 'press_coverage',
              'advisor_joined', 'competitor_exit'];
  };

  // Alert when match found
  alerts: {
    company: string;
    why_interesting: string[];
    match_score: number;
    suggested_action: 'reach_out' | 'monitor' | 'research_more';
    contact_path: string[];  // Who in your network knows them
  };
}
```

### 2. Founder Matching

```typescript
// Recommande des fondateurs a rencontrer base sur ton historique
interface FounderMatching {

  // Learn from your past investments
  yourPattern: {
    successful_investments: Array<{
      founder_traits: string[];
      background_pattern: string;
      what_you_liked: string;
    }>;
  };

  // Find similar founders
  recommendations: Array<{
    founder: string;
    company: string;
    similarity_to_winners: number;
    why_you_might_like: string[];
    potential_concerns: string[];
    intro_path: string[];
  }>;
}
```

### 3. Market Timing Oracle

```typescript
// Signaux macro pour savoir quand deployer vs attendre
interface MarketTimingOracle {

  signals: {
    vc_activity: { trend: string; implication: string };
    public_markets: { trend: string; implication: string };
    exit_activity: { trend: string; implication: string };
    valuation_trends: { trend: string; implication: string };
    dry_powder: { level: string; implication: string };
  };

  recommendation: {
    deploy_aggressively: boolean;
    reasoning: string;
    sectors_to_favor: string[];
    sectors_to_avoid: string[];
  };
}
```

### 4. Portfolio Synergies

```typescript
// Identifie des synergies entre tes participations et un nouveau deal
interface PortfolioSynergies {

  analyze: (newDeal: Deal, portfolio: Company[]) => {
    synergies: Array<{
      portfolio_company: string;
      new_deal: string;

      type: 'customer' | 'partner' | 'technology' | 'talent' | 'market_intel';
      description: string;
      value_potential: 'high' | 'medium' | 'low';

      suggested_intro: string;
    }>;

    conflicts: Array<{
      portfolio_company: string;
      conflict_type: 'competitive' | 'resource' | 'market';
      description: string;
      severity: 'blocker' | 'manageable' | 'minor';
    }>;
  };
}
```

---

## DEALBREAKERS A EVITER

> **"Ce qui tue la confiance dans le produit."**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              DEALBREAKERS                                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  🔴 DONNEES OBSOLETES                                                           │
│     "Si les deals comparables datent de 2022, zero credibilite"                 │
│                                                                                  │
│     → Solution: Refresh quotidien des sources                                   │
│     → Solution: Timestamp visible sur chaque data point                         │
│     → Solution: Alerte si data > 7 jours                                        │
│                                                                                  │
│  🔴 FAUX POSITIFS                                                               │
│     "Si les red flags sont souvent faux, on arrete de les regarder"             │
│                                                                                  │
│     → Solution: Confidence score sur chaque flag                                │
│     → Solution: Ne montrer que flags > 80% confidence                           │
│     → Solution: Track record visible des flags                                  │
│     → Solution: Feedback loop pour ameliorer                                    │
│                                                                                  │
│  🔴 ANALYSES GENERIQUES                                                         │
│     "Si ca ressemble a du ChatGPT basique, zero valeur ajoutee"                 │
│                                                                                  │
│     → Solution: Toujours ancrer dans les comparables                            │
│     → Solution: Chiffres specifiques, pas de generalites                        │
│     → Solution: Context engine oblige                                           │
│     → Solution: "Pas d'analyse sans contexte" = regle absolue                   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

# PARTIE 1 : ARCHITECTURE GLOBALE

## 1.1 Vue d'ensemble

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           FULLINVEST ARCHITECTURE                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                         1. CONTEXT ENGINE                                │    │
│  │                                                                          │    │
│  │   "Avant d'analyser, comprendre l'environnement"                        │    │
│  │                                                                          │    │
│  │   ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐          │    │
│  │   │ Deal       │ │ Market     │ │ People     │ │ Competitive│          │    │
│  │   │ Intelligence│ │ Data      │ │ Graph      │ │ Landscape  │          │    │
│  │   └────────────┘ └────────────┘ └────────────┘ └────────────┘          │    │
│  │         ↑              ↑              ↑              ↑                  │    │
│  │   Crunchbase     Statista      LinkedIn       Web Search               │    │
│  │   Dealroom       Reports       News APIs      News APIs                │    │
│  │   PitchBook      Benchmarks    Court Records  Crunchbase               │    │
│  │                                                                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                      │                                           │
│                                      ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                    2. DOCUMENT INTELLIGENCE                              │    │
│  │                                                                          │    │
│  │   "Extraire et structurer l'information"                                │    │
│  │                                                                          │    │
│  │   ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐          │    │
│  │   │ OCR &      │ │ Claim      │ │ Cross-Doc  │ │ Anomaly    │          │    │
│  │   │ Parse      │ │ Extraction │ │ Validation │ │ Detection  │          │    │
│  │   └────────────┘ └────────────┘ └────────────┘ └────────────┘          │    │
│  │                                                                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                      │                                           │
│                                      ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                    3. MODEL ORCHESTRATION LAYER                          │    │
│  │                                                                          │    │
│  │   "Router intelligemment vers les meilleurs modeles"                    │    │
│  │                                                                          │    │
│  │   ┌─────────────────────────────────────────────────────────────────┐   │    │
│  │   │                     MODEL ROUTER                                 │   │    │
│  │   │                                                                  │   │    │
│  │   │   Task ──→ [Route based on learned performance] ──→ Model(s)   │   │    │
│  │   │                                                                  │   │    │
│  │   │   Available Models (via OpenRouter):                            │   │    │
│  │   │   • Claude (3.5 Sonnet, 3 Opus, 3 Haiku)                       │   │    │
│  │   │   • GPT (4-turbo, 4o, 4o-mini)                                  │   │    │
│  │   │   • Gemini (Pro, Ultra)                                         │   │    │
│  │   │   • Llama (3.1 405B, 70B)                                       │   │    │
│  │   │   • Mistral (Large, Medium)                                     │   │    │
│  │   │   • Perplexity (for web search)                                 │   │    │
│  │   │   • [Any future model]                                          │   │    │
│  │   │                                                                  │   │    │
│  │   └─────────────────────────────────────────────────────────────────┘   │    │
│  │                                                                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                      │                                           │
│                                      ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                         4. AGENT NETWORK                                 │    │
│  │                                                                          │    │
│  │   "27 agents specialises, contexte injecte"                             │    │
│  │                                                                          │    │
│  │   TIER 1: Investigation (parallel)     TIER 2: Synthesis (sequential)   │    │
│  │   ┌─────┬─────┬─────┬─────┬─────┐    ┌─────┬─────┬─────┬─────┬─────┐   │    │
│  │   │ D.S │ D.F │ F.A │ M.I │ C.I │    │ C.D │ S.M │ D.S │ D.A │ M.G │   │    │
│  │   │ T.I │ T.D │ L.R │ C.A │ GTM │    └─────┴─────┴─────┴─────┴─────┘   │    │
│  │   │ C.I │ E.S │ Q.M │     │     │                                       │    │
│  │   └─────┴─────┴─────┴─────┴─────┘    TIER 3: Sector Experts (dynamic)   │    │
│  │                                       ┌─────┬─────┬─────┬─────┬─────┐   │    │
│  │                                       │SaaS │Mkt │Fin │Hlth│Deep│   │    │
│  │                                       │     │plc │tech│tech│tech│...│   │    │
│  │                                       └─────┴─────┴─────┴─────┴─────┘   │    │
│  │                                                                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                      │                                           │
│                                      ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                      5. ENSEMBLE & FUSION LAYER                          │    │
│  │                                                                          │    │
│  │   "Pour les decisions critiques: multi-perspectives"                    │    │
│  │                                                                          │    │
│  │   ┌─────────────────────────────────────────────────────────────────┐   │    │
│  │   │  Question critique (ex: "Should we invest?")                    │   │    │
│  │   │                           │                                      │   │    │
│  │   │           ┌───────────────┼───────────────┐                     │   │    │
│  │   │           ▼               ▼               ▼                     │   │    │
│  │   │      [Model A]       [Model B]       [Model N]                  │   │    │
│  │   │           │               │               │                     │   │    │
│  │   │           └───────────────┼───────────────┘                     │   │    │
│  │   │                           ▼                                      │   │    │
│  │   │                   FUSION ENGINE                                  │   │    │
│  │   │              (Reconcile, Synthesize)                            │   │    │
│  │   │                           │                                      │   │    │
│  │   │                           ▼                                      │   │    │
│  │   │               Unified Analysis + Dissent Notes                  │   │    │
│  │   └─────────────────────────────────────────────────────────────────┘   │    │
│  │                                                                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                      │                                           │
│                                      ▼                                           │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                  6. INTERNAL QUALITY ENGINE (Background)                 │    │
│  │                                                                          │    │
│  │   "Optimisation invisible - l'utilisateur voit la valeur, pas la tuyauterie"│
│  │                                                                          │    │
│  │   • Monitoring qualite des outputs                                      │    │
│  │   • Optimisation automatique du routing                                 │    │
│  │   • Calibration continue des modeles                                    │    │
│  │   • [100% interne - jamais expose au client]                            │    │
│  │                                                                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

# PARTIE 2 : CONTEXT ENGINE

> **"Une analyse sans contexte est une opinion. Une analyse avec contexte est une intelligence."**

## 2.1 Principe Fondamental

```
SANS CONTEXTE                          AVEC CONTEXTE
━━━━━━━━━━━━━                          ━━━━━━━━━━━━━

"La valorisation de                    "La valorisation de 15M€ (30x ARR) se situe
15M€ semble elevee                     dans le percentile 85 des 47 deals Seed SaaS
pour ce stade."                        B2B europeens des 12 derniers mois.

                                       Contexte specifique:
                                       • Median secteur: 18x ARR (source: Dealroom)
                                       • Range P25-P75: 12x - 24x
                                       • [Competitor] a leve a 22x il y a 3 mois
                                       • 4 deals a 25x+ ont fait down round depuis
                                       • Tendance Q4: -15% vs Q3 (cooling market)
                                       • Le fondateur a un track record: exit 12M€ en 2019

                                       Verdict: AGRESSIF vs marche actuel.
                                       Range fair value estime: 10-13M€
                                       Levier de nego: market cooling + down rounds recents"

↓                                      ↓
Opinion generique                      Intelligence actionnable
Pas de credibilite                     Ancre dans la realite
Pas actionnable                        Base de negociation concrete
```

## 2.2 Les 4 Piliers du Context Engine

### PILIER 1 : Deal Intelligence

```typescript
interface DealIntelligence {

  // ═══════════════════════════════════════════════════════════════
  // SIMILAR DEALS - Le coeur du contexte
  // ═══════════════════════════════════════════════════════════════

  similarDeals: {
    // Comment on trouve des deals similaires
    matching_criteria: {
      sector: string;           // Meme secteur
      sub_sector?: string;      // Meme sous-secteur si pertinent
      stage: string;            // Meme stage (+/- 1)
      geography: string;        // Meme region
      business_model: string;   // SaaS, Marketplace, etc.
      time_window: '24_months'; // Deals recents seulement
    };

    // Sources de donnees
    sources: [
      {
        name: 'crunchbase';
        type: 'api';
        data: ['funding_rounds', 'company_info', 'founders'];
        freshness: 'daily';
      },
      {
        name: 'dealroom';
        type: 'api';
        data: ['european_deals', 'valuations', 'investors'];
        freshness: 'daily';
      },
      {
        name: 'pitchbook';
        type: 'api';
        data: ['detailed_financials', 'exits', 'comparables'];
        freshness: 'daily';
      },
      {
        name: 'techcrunch';
        type: 'rss_scrape';
        data: ['announcements', 'funding_news'];
        freshness: 'hourly';
      },
      {
        name: 'internal_database';
        type: 'postgres';
        data: ['our_past_deals', 'outcomes', 'learnings'];
        freshness: 'realtime';
      }
    ];

    // Output structure
    output: {
      deals: Array<{
        company_name: string;
        sector: string;
        stage: string;

        // Funding
        raised_amount: number;
        valuation: number;
        arr_at_raise: number;
        arr_multiple: number;
        date: Date;
        investors: string[];
        lead_investor: string;

        // Current status (critical!)
        current_status: 'thriving' | 'growing' | 'flat' | 'struggling' | 'dead' | 'exited';
        current_arr: number;
        months_since_raise: number;

        // If exited
        exit_info?: {
          type: 'acquisition' | 'ipo' | 'acquihire' | 'shutdown';
          value: number;
          multiple_vs_last_round: number;
          acquirer?: string;
        };

        // Similarity score
        similarity_score: number;  // 0-100
        similarity_factors: string[];
      }>;

      // Aggregated statistics
      statistics: {
        total_deals_found: number;

        valuation_distribution: {
          min: number;
          p25: number;
          median: number;
          p75: number;
          max: number;
        };

        multiple_distribution: {
          min: number;
          p25: number;
          median: number;
          p75: number;
          max: number;
        };

        success_metrics: {
          still_alive_percent: number;
          raised_follow_on_percent: number;
          down_round_percent: number;
          exited_successfully_percent: number;
        };

        common_investors: Array<{
          name: string;
          deal_count: number;
          success_rate: number;
        }>;

        trend: {
          volume_trend: 'increasing' | 'stable' | 'decreasing';
          valuation_trend: 'up' | 'flat' | 'down';
          vs_previous_quarter: number;  // percentage change
        };
      };

      // Specific learnings
      learnings: {
        what_worked: string[];      // Common factors in successful deals
        what_failed: string[];      // Common factors in failed deals
        red_flags_observed: string[];
        success_predictors: string[];
      };
    };
  };

  // ═══════════════════════════════════════════════════════════════
  // FAILURE ANALYSIS - Apprendre des echecs
  // ═══════════════════════════════════════════════════════════════

  failureAnalysis: {
    // Startups similaires qui ont echoue
    failed_companies: Array<{
      name: string;
      sector: string;
      total_raised: number;
      failure_date: Date;
      runway_at_failure: number;

      // Why they failed
      failure_reasons: string[];
      failure_category: 'market' | 'product' | 'team' | 'competition' | 'timing' | 'execution' | 'funding';

      // Warning signs that were visible
      early_warning_signs: string[];

      // What investors wish they'd asked
      questions_we_should_have_asked: string[];

      // Source
      source: string;  // Post-mortem article, investor interview, etc.
    }>;

    // Pattern analysis
    patterns: {
      most_common_failure_reasons: Array<{ reason: string; frequency: number }>;
      average_time_to_failure: number;  // months after last raise
      warning_signs_frequency: Array<{ sign: string; frequency: number }>;
    };
  };

  // ═══════════════════════════════════════════════════════════════
  // EXIT INTELLIGENCE - Comment on sort
  // ═══════════════════════════════════════════════════════════════

  exitIntelligence: {
    recent_exits: Array<{
      company: string;
      sector: string;
      exit_type: 'acquisition' | 'ipo' | 'spac' | 'secondary';
      exit_value: number;
      last_round_valuation: number;
      exit_multiple: number;
      time_to_exit: number;  // months from founding
      acquirer?: string;
      strategic_rationale?: string;
    }>;

    statistics: {
      median_exit_multiple: number;
      median_time_to_exit: number;
      exit_type_distribution: Record<string, number>;
      top_acquirers: string[];
    };

    // IPO pipeline
    ipo_candidates: Array<{
      company: string;
      expected_timeline: string;
      expected_valuation_range: string;
    }>;
  };
}
```

### PILIER 2 : Market Intelligence

```typescript
interface MarketIntelligence {

  // ═══════════════════════════════════════════════════════════════
  // BENCHMARKS - Metriques de reference
  // ═══════════════════════════════════════════════════════════════

  benchmarks: {
    sources: [
      'openview_saas_benchmarks',     // SaaS-specific
      'bessemer_cloud_index',         // Cloud metrics
      'iconiq_growth_benchmarks',     // Growth stage
      'first_round_state_of_startups', // Early stage
      'wing_vc_benchmarks',           // Various
      'internal_portfolio_data'        // Our own data
    ];

    metrics: Array<{
      metric_name: string;           // 'arr_growth_rate', 'net_retention', etc.

      by_stage: {
        pre_seed: MetricDistribution;
        seed: MetricDistribution;
        series_a: MetricDistribution;
        series_b: MetricDistribution;
      };

      by_sector: Record<string, MetricDistribution>;

      trend: {
        current_year: number;
        previous_year: number;
        change: number;
        direction: 'improving' | 'stable' | 'declining';
      };
    }>;
  };

  // ═══════════════════════════════════════════════════════════════
  // MARKET SIZING VALIDATION - Verifier les TAM/SAM/SOM
  // ═══════════════════════════════════════════════════════════════

  marketSizing: {
    sources: [
      'gartner',
      'statista',
      'idc',
      'forrester',
      'grandview_research',
      'markets_and_markets'
    ];

    // Pour un secteur donne
    sectorData: {
      sector: string;

      tam: {
        value: number;
        year: number;
        source: string;
        methodology: 'top_down' | 'bottom_up';
        confidence: 'high' | 'medium' | 'low';
      };

      growth_rate: {
        cagr: number;
        period: string;
        source: string;
      };

      key_trends: string[];
      key_drivers: string[];
      key_risks: string[];

      regional_breakdown: Record<string, number>;
    };
  };

  // ═══════════════════════════════════════════════════════════════
  // MACRO SIGNALS - Contexte macro
  // ═══════════════════════════════════════════════════════════════

  macroSignals: {
    vc_market: {
      total_dry_powder: number;        // Capital disponible
      deployment_pace: 'fast' | 'normal' | 'slow';
      fundraising_environment: 'hot' | 'warm' | 'cool' | 'cold';

      by_stage: Record<string, {
        activity_level: string;
        valuation_trend: string;
      }>;
    };

    public_markets: {
      relevant_indices: Array<{
        name: string;       // 'NASDAQ', 'BVP Cloud Index', etc.
        current: number;
        ytd_change: number;
        trend: string;
      }>;

      comparable_multiples: Array<{
        company: string;
        multiple: number;
        trend: 'up' | 'flat' | 'down';
      }>;
    };

    sentiment: {
      overall: 'bullish' | 'neutral' | 'bearish';
      sector_specific: 'bullish' | 'neutral' | 'bearish';
      key_concerns: string[];
      key_opportunities: string[];
    };
  };
}
```

### PILIER 3 : People Intelligence

```typescript
interface PeopleIntelligence {

  // ═══════════════════════════════════════════════════════════════
  // FOUNDER DEEP BACKGROUND
  // ═══════════════════════════════════════════════════════════════

  founderIntelligence: {
    // Pour chaque fondateur
    founders: Array<{
      name: string;
      role: string;

      // ─────────────────────────────────────────────────────────
      // Previous ventures (CRITICAL)
      // ─────────────────────────────────────────────────────────
      previousVentures: Array<{
        company: string;
        role: string;
        dates: { start: Date; end: Date };

        outcome: {
          status: 'big_success' | 'success' | 'acquihire' | 'pivot' | 'failure' | 'ongoing';
          exit_value?: number;
          details: string;
        };

        funding: {
          total_raised: number;
          investors: string[];
        };

        // What we found
        public_info: Array<{
          type: 'news_article' | 'interview' | 'podcast' | 'linkedin' | 'crunchbase';
          title: string;
          url: string;
          summary: string;
          sentiment: 'positive' | 'neutral' | 'negative';
        }>;

        // Red flags if any
        concerns: string[];
      }>;

      // ─────────────────────────────────────────────────────────
      // Professional background verification
      // ─────────────────────────────────────────────────────────
      professionalBackground: {
        claimed: Array<{
          company: string;
          role: string;
          dates: string;
        }>;

        verified: Array<{
          company: string;
          role: string;
          dates: string;
          verification_source: 'linkedin' | 'company_website' | 'press' | 'reference';
          confidence: 'high' | 'medium' | 'low';
        }>;

        discrepancies: Array<{
          claimed: string;
          found: string;
          severity: 'critical' | 'notable' | 'minor';
        }>;
      };

      // ─────────────────────────────────────────────────────────
      // Education verification
      // ─────────────────────────────────────────────────────────
      education: {
        claimed: Education[];
        verified: Education[];
        discrepancies: string[];
      };

      // ─────────────────────────────────────────────────────────
      // Public presence & reputation
      // ─────────────────────────────────────────────────────────
      publicPresence: {
        linkedin: {
          url: string;
          connections: number;
          endorsements_relevant: number;
          recommendations_count: number;
        };

        twitter: {
          handle: string;
          followers: number;
          engagement_rate: number;
          topics: string[];
          sentiment: 'positive' | 'neutral' | 'controversial';
        };

        thought_leadership: {
          articles_written: number;
          speaking_engagements: string[];
          podcast_appearances: string[];
          notable_quotes: string[];
        };

        media_coverage: Array<{
          outlet: string;
          title: string;
          date: Date;
          sentiment: 'positive' | 'neutral' | 'negative';
          url: string;
        }>;
      };

      // ─────────────────────────────────────────────────────────
      // Red flags search (CRITICAL)
      // ─────────────────────────────────────────────────────────
      redFlagsSearch: {
        legal: {
          lawsuits: Array<{
            case: string;
            status: string;
            details: string;
            source: string;
          }>;
          bankruptcies: string[];
          regulatory_issues: string[];
        };

        professional: {
          glassdoor_mentions: Array<{
            company: string;
            sentiment: string;
            quote: string;
          }>;
          controversies: Array<{
            description: string;
            source: string;
            severity: string;
          }>;
        };

        // Summary
        red_flags_found: string[];
        confidence_in_search: 'thorough' | 'partial' | 'limited';
      };

      // ─────────────────────────────────────────────────────────
      // Network analysis
      // ─────────────────────────────────────────────────────────
      network: {
        notable_connections: Array<{
          name: string;
          relationship: string;
          relevance: string;
        }>;

        previous_colleagues_now_successful: Array<{
          name: string;
          current_company: string;
          relationship: string;
        }>;

        investors_who_know_them: Array<{
          investor: string;
          context: string;
          potential_reference: boolean;
        }>;
      };
    }>;

    // ─────────────────────────────────────────────────────────
    // Team dynamics analysis
    // ─────────────────────────────────────────────────────────
    teamDynamics: {
      composition: {
        roles_filled: string[];
        roles_missing: string[];
        experience_balance: 'senior_heavy' | 'balanced' | 'junior_heavy';
      };

      working_history: {
        founders_worked_together_before: boolean;
        duration_working_together: number;  // months
        previous_collaboration_details: string;
      };

      comparison_to_successful_teams: {
        typical_winning_team_composition: string[];
        this_team_matches: string[];
        this_team_gaps: string[];
      };
    };
  };

  // ═══════════════════════════════════════════════════════════════
  // EMPLOYEE SIGNALS
  // ═══════════════════════════════════════════════════════════════

  employeeSignals: {
    glassdoor: {
      rating: number;
      review_count: number;
      trend: 'improving' | 'stable' | 'declining';

      pros_common: string[];
      cons_common: string[];

      ceo_approval: number;
      recommend_to_friend: number;
    };

    linkedin_signals: {
      employee_count: number;
      growth_rate: number;  // % over 6 months

      recent_hires: Array<{
        role: string;
        seniority: string;
        previous_company: string;
      }>;

      recent_departures: Array<{
        role: string;
        tenure: number;
        new_company: string;
      }>;

      turnover_signal: 'healthy' | 'concerning' | 'red_flag';
    };
  };
}
```

### PILIER 4 : Competitive Intelligence

```typescript
interface CompetitiveIntelligence {

  // ═══════════════════════════════════════════════════════════════
  // COMPETITOR LANDSCAPE
  // ═══════════════════════════════════════════════════════════════

  competitorLandscape: {
    // For each competitor
    competitors: Array<{
      name: string;
      website: string;
      description: string;

      // Classification
      type: 'direct' | 'indirect' | 'potential' | 'big_tech' | 'adjacent';
      threat_level: 'critical' | 'high' | 'medium' | 'low';
      mentioned_in_deck: boolean;

      // Company info
      founded: number;
      hq: string;
      employee_count: number;
      employee_growth_6m: number;

      // Funding
      funding: {
        total_raised: number;
        last_round: {
          amount: number;
          date: Date;
          type: string;
          valuation?: number;
          investors: string[];
        };
        runway_estimate: 'long' | 'medium' | 'short' | 'unknown';
      };

      // Traction signals
      traction: {
        web_traffic_monthly: number;
        web_traffic_trend: 'up' | 'flat' | 'down';
        app_downloads?: number;
        social_following: number;
        job_postings: number;
      };

      // Product
      product: {
        key_features: string[];
        pricing_model: string;
        pricing_range: string;
        target_customer: string;
      };

      // Recent activity
      recent_news: Array<{
        title: string;
        date: Date;
        type: 'funding' | 'product' | 'partnership' | 'executive' | 'other';
        summary: string;
        url: string;
      }>;

      // Competitive position
      strengths: string[];
      weaknesses: string[];
      vs_target_company: {
        areas_they_win: string[];
        areas_target_wins: string[];
        key_differentiators: string[];
      };
    }>;

    // ─────────────────────────────────────────────────────────
    // Market dynamics
    // ─────────────────────────────────────────────────────────
    marketDynamics: {
      market_structure: 'fragmented' | 'consolidating' | 'oligopoly' | 'monopoly';

      leader: {
        name: string;
        market_share_estimate: number;
        why_they_lead: string[];
      };

      recent_consolidation: Array<{
        acquirer: string;
        target: string;
        date: Date;
        rationale: string;
      }>;

      barriers_to_entry: {
        level: 'high' | 'medium' | 'low';
        factors: string[];
      };

      winner_take_all: {
        likelihood: 'high' | 'medium' | 'low';
        reasoning: string;
      };
    };

    // ─────────────────────────────────────────────────────────
    // Big Tech threat
    // ─────────────────────────────────────────────────────────
    bigTechThreat: {
      overall_risk: 'critical' | 'high' | 'medium' | 'low';

      by_player: Array<{
        company: 'Google' | 'Amazon' | 'Microsoft' | 'Apple' | 'Meta' | 'Salesforce';

        current_presence: {
          has_competing_product: boolean;
          product_name?: string;
          market_share?: number;
        };

        likelihood_of_entry: 'high' | 'medium' | 'low';

        signals: {
          acquisitions_in_space: string[];
          job_postings: string[];
          patents_filed: string[];
          announcements: string[];
        };

        impact_if_enters: 'existential' | 'severe' | 'moderate' | 'manageable';

        defensibility: string[];  // Why target could survive
      }>;
    };
  };
}
```

## 2.3 Context Engine Implementation

```typescript
// ═══════════════════════════════════════════════════════════════════════════
// CONTEXT ENGINE - Core Implementation
// ═══════════════════════════════════════════════════════════════════════════

interface ContextEngine {

  // ─────────────────────────────────────────────────────────────────────────
  // CONFIGURATION
  // ─────────────────────────────────────────────────────────────────────────

  config: {
    // Data sources (pluggable)
    dataSources: Array<{
      name: string;
      type: 'api' | 'scrape' | 'database' | 'manual';
      credentials: string;  // env var name
      enabled: boolean;
      priority: number;     // for fallbacks
      rate_limit: number;   // requests per minute
      cost_per_request?: number;
    }>;

    // Caching
    cache: {
      enabled: true;
      ttl: {
        deal_data: 24 * 60 * 60;      // 24 hours
        market_data: 7 * 24 * 60 * 60; // 7 days
        benchmarks: 30 * 24 * 60 * 60; // 30 days
        people_data: 24 * 60 * 60;     // 24 hours
      };
      storage: 'redis' | 'postgres';
    };

    // Quality thresholds
    quality: {
      min_similar_deals: 10;         // Minimum deals for statistics
      min_confidence_score: 0.7;     // Minimum confidence to include
      max_data_age_days: 90;         // Maximum age for deal data
    };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // CONTEXT BUILDING PIPELINE
  // ─────────────────────────────────────────────────────────────────────────

  buildContext: async (deal: Deal) => {
    // STEP 1: Parallel data fetching
    const [
      similarDeals,
      marketData,
      founderData,
      competitorData,
      benchmarks
    ] = await Promise.all([
      fetchSimilarDeals(deal),
      fetchMarketData(deal.sector),
      fetchFounderBackground(deal.founders),
      fetchCompetitorLandscape(deal.sector, deal.competitors_mentioned),
      fetchBenchmarks(deal.sector, deal.stage)
    ]);

    // STEP 2: Enrich and cross-reference
    const enrichedContext = {
      // Similar deals with outcome analysis
      similar_deals: {
        deals: similarDeals,
        statistics: computeStatistics(similarDeals),
        learnings: extractLearnings(similarDeals),
        relevance_to_this_deal: computeRelevance(deal, similarDeals)
      },

      // Market context
      market: {
        sizing: marketData.sizing,
        trends: marketData.trends,
        benchmarks: benchmarks,
        macro: marketData.macro,
        timing_assessment: assessTiming(deal, marketData)
      },

      // People context
      people: {
        founders: founderData,
        team_assessment: assessTeam(deal, founderData),
        network_strength: assessNetwork(founderData)
      },

      // Competitive context
      competition: {
        landscape: competitorData,
        positioning: assessPositioning(deal, competitorData),
        threat_level: assessThreatLevel(competitorData)
      },

      // Meta
      context_quality: {
        completeness: assessCompleteness(enrichedContext),
        confidence: assessConfidence(enrichedContext),
        gaps: identifyGaps(enrichedContext),
        freshness: assessFreshness(enrichedContext)
      }
    };

    // STEP 3: Generate context summary for prompts
    const contextSummary = generateContextSummary(enrichedContext);

    return {
      full: enrichedContext,
      summary: contextSummary,
      for_prompt: formatContextForPrompt(enrichedContext)
    };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // CONTEXT INJECTION INTO PROMPTS
  // ─────────────────────────────────────────────────────────────────────────

  injectContext: (basePrompt: string, context: EnrichedContext, task: string) => {

    // Select relevant context based on task
    const relevantContext = selectRelevantContext(context, task);

    const contextBlock = `
## CONTEXTE DE MARCHE (donnees temps reel)

### Deals similaires (${relevantContext.similar_deals.count} deals, ${relevantContext.similar_deals.time_window})

**Statistiques cles:**
- Valorisation mediane: ${relevantContext.similar_deals.stats.median_valuation}€
- Multiple ARR median: ${relevantContext.similar_deals.stats.median_multiple}x
- Range P25-P75: ${relevantContext.similar_deals.stats.p25_multiple}x - ${relevantContext.similar_deals.stats.p75_multiple}x
- Taux de survie: ${relevantContext.similar_deals.stats.survival_rate}%
- Taux de down round: ${relevantContext.similar_deals.stats.down_round_rate}%

**Deals notables recents:**
${relevantContext.similar_deals.notable.map(d =>
  `- ${d.company}: ${d.raised}€ @ ${d.multiple}x (${d.status})`
).join('\n')}

**Tendance:** ${relevantContext.similar_deals.trend}

### Benchmarks secteur (${relevantContext.benchmarks.source})

| Metrique | Ce deal | P25 | Median | P75 | Top 10% |
|----------|---------|-----|--------|-----|---------|
${relevantContext.benchmarks.metrics.map(m =>
  `| ${m.name} | ${m.deal_value} | ${m.p25} | ${m.median} | ${m.p75} | ${m.p90} |`
).join('\n')}

### Paysage concurrentiel

**Leader:** ${relevantContext.competition.leader} (${relevantContext.competition.leader_funding} leves)

**Activite recente:**
${relevantContext.competition.recent_activity.map(a =>
  `- ${a.company}: ${a.event} (${a.date})`
).join('\n')}

**Big Tech threat:** ${relevantContext.competition.big_tech_risk}

### Background fondateurs

${relevantContext.founders.map(f => `
**${f.name}** (${f.role})
- Track record: ${f.previous_ventures_summary}
- Red flags: ${f.red_flags.length > 0 ? f.red_flags.join(', ') : 'Aucun identifie'}
- Network: ${f.network_strength}
`).join('\n')}

---

Utilise ce contexte pour ancrer ton analyse dans la realite du marche.
Compare systematiquement aux deals similaires et aux benchmarks.

`;

    return contextBlock + '\n\n' + basePrompt;
  };
}
```

---

# PARTIE 3 : MODEL ORCHESTRATION LAYER

> **"Pas de modele hardcode. Performance apprise. Evolution continue."**

## 3.1 Principes

```
MAUVAIS                                BON
━━━━━━━                                ━━━

"Claude = finance"                     models = [configurable]
"GPT = narrative"                      routing = learned_from_performance
"Gemini = research"                    ensemble = dynamic

→ Presomptions                         → Empirique
→ Statique                             → Evolutif
→ Fragile                              → Robust
```

## 3.2 Model Router Architecture

```typescript
// ═══════════════════════════════════════════════════════════════════════════
// MODEL ROUTER - Model-Agnostic, Learning-Based
// ═══════════════════════════════════════════════════════════════════════════

interface ModelRouter {

  // ─────────────────────────────────────────────────────────────────────────
  // AVAILABLE MODELS (via OpenRouter)
  // ─────────────────────────────────────────────────────────────────────────

  models: {
    // The router doesn't care what models exist
    // It discovers them from OpenRouter and learns their strengths

    registry: Array<{
      id: string;                    // 'anthropic/claude-3-opus'
      provider: string;              // 'anthropic'
      name: string;                  // 'Claude 3 Opus'

      // Capabilities (from OpenRouter)
      capabilities: {
        context_window: number;
        supports_vision: boolean;
        supports_function_calling: boolean;
        supports_streaming: boolean;
      };

      // Pricing (from OpenRouter)
      pricing: {
        input_per_1k: number;
        output_per_1k: number;
      };

      // Learned performance (our data)
      learned_performance: {
        // Per task type
        by_task: Record<TaskType, {
          sample_size: number;
          avg_quality_score: number;    // 0-100, from human feedback
          avg_latency_ms: number;
          success_rate: number;
          cost_efficiency: number;       // quality / cost
        }>;

        // Overall
        overall_quality: number;
        overall_reliability: number;
        last_evaluated: Date;
      };

      // Status
      status: 'active' | 'degraded' | 'down';
      last_health_check: Date;
    }>;

    // Auto-discovery
    discovery: {
      source: 'openrouter_api';
      refresh_interval: 24 * 60 * 60;  // Daily
      auto_add_new_models: true;
      min_evaluation_before_production: 50;  // samples
    };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // ROUTING STRATEGIES
  // ─────────────────────────────────────────────────────────────────────────

  strategies: {

    // Strategy 1: Best quality (regardless of cost)
    quality_first: {
      select: (task: TaskType, models: Model[]) => {
        return models
          .filter(m => m.status === 'active')
          .filter(m => m.learned_performance.by_task[task]?.sample_size > 20)
          .sort((a, b) =>
            b.learned_performance.by_task[task].avg_quality_score -
            a.learned_performance.by_task[task].avg_quality_score
          )[0];
      };
    };

    // Strategy 2: Best cost efficiency
    cost_optimized: {
      select: (task: TaskType, models: Model[]) => {
        return models
          .filter(m => m.status === 'active')
          .filter(m => m.learned_performance.by_task[task]?.avg_quality_score > 70)
          .sort((a, b) =>
            b.learned_performance.by_task[task].cost_efficiency -
            a.learned_performance.by_task[task].cost_efficiency
          )[0];
      };
    };

    // Strategy 3: Fastest (for interactive features)
    speed_first: {
      select: (task: TaskType, models: Model[]) => {
        return models
          .filter(m => m.status === 'active')
          .filter(m => m.learned_performance.by_task[task]?.avg_quality_score > 60)
          .sort((a, b) =>
            a.learned_performance.by_task[task].avg_latency_ms -
            b.learned_performance.by_task[task].avg_latency_ms
          )[0];
      };
    };

    // Strategy 4: Balanced (default)
    balanced: {
      select: (task: TaskType, models: Model[]) => {
        // Weighted score combining quality, cost, speed
        const score = (m: Model) => {
          const perf = m.learned_performance.by_task[task];
          if (!perf || perf.sample_size < 10) return 0;

          return (
            perf.avg_quality_score * 0.5 +
            perf.cost_efficiency * 0.3 +
            (1000 / perf.avg_latency_ms) * 0.2
          );
        };

        return models
          .filter(m => m.status === 'active')
          .sort((a, b) => score(b) - score(a))[0];
      };
    };

    // Strategy 5: Exploration (try new/under-sampled models)
    exploration: {
      probability: 0.1;  // 10% of requests
      select: (task: TaskType, models: Model[]) => {
        const underSampled = models.filter(m =>
          m.status === 'active' &&
          (m.learned_performance.by_task[task]?.sample_size || 0) < 50
        );

        if (underSampled.length > 0) {
          return underSampled[Math.floor(Math.random() * underSampled.length)];
        }
        return null;  // Fall back to balanced
      };
    };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // TASK TYPES
  // ─────────────────────────────────────────────────────────────────────────

  taskTypes: {
    // Document processing
    'ocr_extraction': { requires_vision: true };
    'claim_extraction': { requires_long_context: true };
    'document_summary': {};

    // Analysis tasks
    'financial_analysis': { complexity: 'high' };
    'market_analysis': { complexity: 'high' };
    'team_analysis': { complexity: 'medium' };
    'competitive_analysis': { complexity: 'high' };
    'legal_analysis': { complexity: 'high' };
    'technical_analysis': { complexity: 'high' };

    // Synthesis tasks
    'contradiction_detection': { requires_reasoning: true };
    'scenario_modeling': { requires_reasoning: true };
    'scoring': { requires_consistency: true };
    'recommendation': { requires_reasoning: true };

    // Generation tasks
    'question_generation': { creativity: 'medium' };
    'memo_writing': { creativity: 'high' };
    'summary_writing': { creativity: 'low' };

    // Web research
    'web_search_synthesis': { requires_web: true };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // ROUTING LOGIC
  // ─────────────────────────────────────────────────────────────────────────

  route: async (request: {
    task: TaskType;
    input: string;
    strategy?: RoutingStrategy;
    constraints?: {
      max_cost?: number;
      max_latency_ms?: number;
      min_quality?: number;
      preferred_models?: string[];
      excluded_models?: string[];
    };
  }) => {

    const strategy = request.strategy || 'balanced';

    // Filter models by constraints
    let candidates = this.models.registry
      .filter(m => m.status === 'active')
      .filter(m => !request.constraints?.excluded_models?.includes(m.id))
      .filter(m => {
        if (request.constraints?.preferred_models?.length) {
          return request.constraints.preferred_models.includes(m.id);
        }
        return true;
      });

    // Apply task-specific requirements
    const taskConfig = this.taskTypes[request.task];
    if (taskConfig.requires_vision) {
      candidates = candidates.filter(m => m.capabilities.supports_vision);
    }
    if (taskConfig.requires_long_context) {
      candidates = candidates.filter(m => m.capabilities.context_window > 100000);
    }

    // Exploration: occasionally try under-sampled models
    if (Math.random() < this.strategies.exploration.probability) {
      const exploration = this.strategies.exploration.select(request.task, candidates);
      if (exploration) {
        return { model: exploration, reason: 'exploration' };
      }
    }

    // Apply selected strategy
    const selected = this.strategies[strategy].select(request.task, candidates);

    if (!selected) {
      throw new Error(`No suitable model found for task ${request.task}`);
    }

    return { model: selected, reason: strategy };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // FALLBACK HANDLING
  // ─────────────────────────────────────────────────────────────────────────

  fallback: {
    enabled: true;

    triggers: [
      'rate_limit_exceeded',
      'timeout',
      'server_error',
      'context_length_exceeded',
      'content_filter'
    ];

    strategy: async (failedModel: Model, task: TaskType, error: Error) => {
      // Mark model as potentially degraded
      await this.updateModelStatus(failedModel.id, 'degraded');

      // Find alternative
      const alternatives = this.models.registry
        .filter(m => m.id !== failedModel.id)
        .filter(m => m.status === 'active');

      // Route to next best
      return this.strategies.balanced.select(task, alternatives);
    };

    maxRetries: 3;
    backoffMs: [1000, 2000, 4000];
  };
}
```

## 3.3 Ensemble & Fusion Layer

```typescript
// ═══════════════════════════════════════════════════════════════════════════
// ENSEMBLE & FUSION - Multi-Model Analysis
// ═══════════════════════════════════════════════════════════════════════════

interface EnsembleLayer {

  // ─────────────────────────────────────────────────────────────────────────
  // CONFIGURATION
  // ─────────────────────────────────────────────────────────────────────────

  config: {
    // Which questions warrant multi-model analysis
    critical_questions: [
      'investment_recommendation',
      'valuation_assessment',
      'team_evaluation',
      'risk_assessment',
      'competitive_moat'
    ];

    // How many models to use
    ensemble_size: {
      default: 3;
      max: 5;
      min: 2;
    };

    // Model selection for ensemble (diverse perspectives)
    model_selection: {
      strategy: 'maximize_diversity';

      // Diversity factors (learned from historical disagreements)
      diversity_matrix: {
        // This is LEARNED, not hardcoded
        // Represents how often models disagree on various tasks
        // Higher = more diverse perspectives
        // Updated continuously from actual disagreements
      };
    };

    // Cost control
    cost_multiplier_accepted: 3;  // Accept 3x cost for critical questions
  };

  // ─────────────────────────────────────────────────────────────────────────
  // ENSEMBLE EXECUTION
  // ─────────────────────────────────────────────────────────────────────────

  runEnsemble: async (request: {
    question: string;
    context: EnrichedContext;
    question_type: CriticalQuestionType;
  }) => {

    // STEP 1: Select diverse models
    const models = await selectDiverseModels(
      request.question_type,
      this.config.ensemble_size.default
    );

    // STEP 2: Run in parallel
    const responses = await Promise.all(
      models.map(model =>
        executeWithModel(model, request.question, request.context)
      )
    );

    // STEP 3: Analyze responses
    const analysis = analyzeResponses(responses);

    return {
      individual_responses: responses.map(r => ({
        model: r.model,
        response: r.response,
        confidence: r.confidence,
        key_points: r.key_points
      })),

      analysis: analysis
    };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // RESPONSE ANALYSIS
  // ─────────────────────────────────────────────────────────────────────────

  analyzeResponses: (responses: ModelResponse[]) => {

    return {
      // What do they agree on?
      consensus: {
        points: extractConsensusPoints(responses),
        confidence: calculateConsensusConfidence(responses),
        strength: 'strong' | 'moderate' | 'weak'
      };

      // What do they disagree on?
      disagreements: {
        points: extractDisagreementPoints(responses),
        by_model: responses.map(r => ({
          model: r.model,
          position: r.position,
          reasoning: r.reasoning
        })),
        significance: 'critical' | 'notable' | 'minor'
      };

      // Unique insights (one model saw something others missed)
      unique_insights: responses.flatMap(r =>
        r.key_points.filter(p =>
          !otherResponsesContain(responses, r.model, p)
        )
      );

      // Overall assessment
      overall: {
        direction: determineOverallDirection(responses),
        confidence: calculateOverallConfidence(responses),
        needs_human_review: responses.length === 0 ||
          analysis.disagreements.significance === 'critical'
      };
    };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // FUSION ENGINE
  // ─────────────────────────────────────────────────────────────────────────

  fuseResponses: async (analysis: ResponseAnalysis, context: EnrichedContext) => {

    // Use a capable model to synthesize
    const fusionModel = await this.modelRouter.route({
      task: 'synthesis',
      strategy: 'quality_first'
    });

    const fusionPrompt = `
Tu es un meta-analyste. Tu recois les analyses de ${analysis.individual_responses.length}
modeles differents sur la meme question. Ton role:

## LES ANALYSES RECUES

${analysis.individual_responses.map((r, i) => `
### Analyse ${i + 1} (${r.model})

${r.response}

Points cles: ${r.key_points.join(', ')}
Confidence: ${r.confidence}
`).join('\n\n')}

## ANALYSE DES REPONSES

**Points de consensus** (${analysis.consensus.points.length}):
${analysis.consensus.points.map(p => `- ${p}`).join('\n')}

**Points de desaccord** (${analysis.disagreements.points.length}):
${analysis.disagreements.points.map(p => `- ${p}`).join('\n')}

**Insights uniques**:
${analysis.unique_insights.map(i => `- [${i.model}] ${i.insight}`).join('\n')}

## TA MISSION

1. **Synthetise** une reponse unifiee qui capture le meilleur de chaque analyse
2. **Explique** les desaccords et qui a probablement raison (avec justification)
3. **Identifie** les questions qui restent ouvertes et necessitent investigation humaine
4. **Conclus** avec une recommandation claire et un niveau de confiance

## FORMAT DE SORTIE

### SYNTHESE

[Ta synthese unifiee]

### ANALYSE DES DESACCORDS

Pour chaque desaccord:
- **Point**: [Le point de desaccord]
- **Positions**: [Qui dit quoi]
- **Mon analyse**: [Qui a probablement raison et pourquoi]

### QUESTIONS OUVERTES

[Questions qui necessitent investigation humaine]

### RECOMMANDATION FINALE

**Verdict**: [Clair et actionnable]
**Confiance**: [Haute/Moyenne/Basse]
**Justification**: [Pourquoi]
`;

    const fusion = await callModel(fusionModel.model, fusionPrompt);

    return {
      synthesized_response: fusion.response,
      consensus_strength: analysis.consensus.strength,
      disagreement_analysis: fusion.disagreement_analysis,
      open_questions: fusion.open_questions,
      final_recommendation: fusion.recommendation,
      confidence: fusion.confidence,

      // Audit trail
      models_used: analysis.individual_responses.map(r => r.model),
      fusion_model: fusionModel.model.id
    };
  };
}
```

## 3.4 Internal Quality Engine (Background)

> **Note importante**: Cette section decrit des mecanismes 100% internes.
> L'utilisateur ne voit jamais cette complexite - il voit simplement des resultats de qualite.
> C'est la "tuyauterie" qui fait que le systeme fonctionne bien, pas un argument de vente.

```typescript
// ═══════════════════════════════════════════════════════════════════════════
// INTERNAL QUALITY ENGINE - Background Optimization (invisible to users)
// ═══════════════════════════════════════════════════════════════════════════

interface InternalQualityEngine {

  // ─────────────────────────────────────────────────────────────────────────
  // PERFORMANCE TRACKING
  // ─────────────────────────────────────────────────────────────────────────

  tracking: {
    // Every model call is logged
    logModelCall: (call: {
      model_id: string;
      task_type: TaskType;
      input_tokens: number;
      output_tokens: number;
      latency_ms: number;
      cost: number;
      success: boolean;
      error?: string;
    }) => void;

    // Quality feedback (from users or automated)
    logQualityFeedback: (feedback: {
      model_id: string;
      task_type: TaskType;
      call_id: string;

      // Ratings
      quality_score: number;        // 1-5 or 1-100
      accuracy_score?: number;      // If verifiable
      helpfulness_score?: number;

      // Source
      feedback_source: 'user_explicit' | 'user_implicit' | 'automated' | 'outcome';

      // Details
      comments?: string;
      corrections?: string;
    }) => void;

    // Outcome tracking (for investment decisions)
    logOutcome: (outcome: {
      deal_id: string;

      // What we predicted
      predictions: {
        recommendation: string;
        confidence: number;
        key_risks: string[];
        expected_multiple: number;
      };

      // What happened
      actual: {
        invested: boolean;
        current_status?: string;
        current_multiple?: number;
        learnings?: string[];
      };

      // Time
      prediction_date: Date;
      outcome_date: Date;
    }) => void;
  };

  // ─────────────────────────────────────────────────────────────────────────
  // PERFORMANCE AGGREGATION
  // ─────────────────────────────────────────────────────────────────────────

  aggregation: {
    // Run daily
    aggregatePerformance: async () => {
      for (const model of models) {
        for (const taskType of taskTypes) {
          const metrics = await calculateMetrics(model.id, taskType);

          await updateModelPerformance(model.id, taskType, {
            sample_size: metrics.sample_size,
            avg_quality_score: metrics.avg_quality,
            avg_latency_ms: metrics.avg_latency,
            success_rate: metrics.success_rate,
            cost_efficiency: metrics.avg_quality / metrics.avg_cost,
            trend: calculateTrend(metrics)
          });
        }
      }
    };

    // Detect performance changes
    detectAnomalies: async () => {
      const anomalies = [];

      for (const model of models) {
        const recent = await getRecentPerformance(model.id, '7d');
        const baseline = await getBaselinePerformance(model.id, '30d');

        // Quality drop?
        if (recent.avg_quality < baseline.avg_quality * 0.9) {
          anomalies.push({
            model: model.id,
            type: 'quality_drop',
            magnitude: (baseline.avg_quality - recent.avg_quality) / baseline.avg_quality,
            recommendation: 'reduce_traffic'
          });
        }

        // Latency spike?
        if (recent.avg_latency > baseline.avg_latency * 1.5) {
          anomalies.push({
            model: model.id,
            type: 'latency_spike',
            magnitude: (recent.avg_latency - baseline.avg_latency) / baseline.avg_latency,
            recommendation: 'monitor'
          });
        }
      }

      return anomalies;
    };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // MODEL DIVERSITY LEARNING
  // ─────────────────────────────────────────────────────────────────────────

  diversityLearning: {
    // Track when models disagree
    trackDisagreement: (analysis: ResponseAnalysis) => {
      const disagreementPairs = [];

      for (let i = 0; i < analysis.individual_responses.length; i++) {
        for (let j = i + 1; j < analysis.individual_responses.length; j++) {
          const modelA = analysis.individual_responses[i].model;
          const modelB = analysis.individual_responses[j].model;
          const disagreed = hasDisagreement(
            analysis.individual_responses[i],
            analysis.individual_responses[j]
          );

          disagreementPairs.push({
            model_a: modelA,
            model_b: modelB,
            disagreed: disagreed,
            task_type: analysis.task_type
          });
        }
      }

      // Update diversity matrix
      for (const pair of disagreementPairs) {
        await updateDiversityMatrix(
          pair.model_a,
          pair.model_b,
          pair.disagreed,
          pair.task_type
        );
      }
    };

    // Use diversity matrix for ensemble selection
    selectDiverseModels: async (taskType: TaskType, count: number) => {
      const diversityMatrix = await getDiversityMatrix(taskType);

      // Greedy selection maximizing pairwise diversity
      const selected = [];
      const candidates = await getActiveModels();

      // Start with best performing model
      selected.push(
        candidates.sort((a, b) =>
          b.learned_performance.by_task[taskType]?.avg_quality_score -
          a.learned_performance.by_task[taskType]?.avg_quality_score
        )[0]
      );

      // Add models that maximize diversity from selected set
      while (selected.length < count && candidates.length > 0) {
        let maxDiversity = -1;
        let nextModel = null;

        for (const candidate of candidates) {
          if (selected.includes(candidate)) continue;

          const diversity = selected.reduce((sum, s) =>
            sum + (diversityMatrix[s.id]?.[candidate.id] || 0.5), 0
          ) / selected.length;

          if (diversity > maxDiversity) {
            maxDiversity = diversity;
            nextModel = candidate;
          }
        }

        if (nextModel) selected.push(nextModel);
      }

      return selected;
    };
  };

  // ─────────────────────────────────────────────────────────────────────────
  // PREDICTION CALIBRATION
  // ─────────────────────────────────────────────────────────────────────────

  calibration: {
    // "When we predict X confidence, how often are we right?"
    calculateCalibration: async () => {
      const outcomes = await getOutcomesWithPredictions();

      const buckets = {
        '90-100': { predicted: 0, correct: 0 },
        '70-89': { predicted: 0, correct: 0 },
        '50-69': { predicted: 0, correct: 0 },
        '30-49': { predicted: 0, correct: 0 },
        '0-29': { predicted: 0, correct: 0 }
      };

      for (const outcome of outcomes) {
        const bucket = getBucket(outcome.predicted_confidence);
        buckets[bucket].predicted++;
        if (outcome.was_correct) buckets[bucket].correct++;
      }

      return {
        calibration: Object.entries(buckets).map(([range, data]) => ({
          confidence_range: range,
          predicted_count: data.predicted,
          actual_accuracy: data.correct / data.predicted,
          expected_accuracy: getRangeMidpoint(range) / 100,
          calibration_error: Math.abs(
            data.correct / data.predicted - getRangeMidpoint(range) / 100
          )
        })),

        overall_calibration_error: calculateOverallCalibration(buckets),

        recommendation: generateCalibrationRecommendation(buckets)
      };
    };

    // Adjust confidence scores based on calibration
    adjustConfidence: (rawConfidence: number) => {
      const calibration = await getLatestCalibration();
      return applyCalibrationCurve(rawConfidence, calibration);
    };
  };
}
```

---

# PARTIE 4 : AGENT NETWORK

## 4.1 Les 27 Agents

### TIER 1 : Investigation Agents (13 agents, parallel)

| # | Agent | Emoji | Mission | Context Required |
|---|-------|-------|---------|------------------|
| 1 | Deal Screener | 📥 | Qualification rapide | Similar deals, benchmarks |
| 2 | Deck Forensics | 📊 | Analyse forensique du deck | Competitor decks patterns |
| 3 | Financial Auditor | 💰 | Audit des chiffres | Benchmarks, comparable financials |
| 4 | Market Intelligence | 🔍 | Verification claims marche | Market data, reports |
| 5 | Competitive Intel | ⚔️ | Paysage concurrentiel | Competitor database |
| 6 | Team Investigator | 👥 | Background check equipe | People graph, history |
| 7 | Technical DD | 🔧 | Evaluation technique | Tech trends, architecture patterns |
| 8 | Legal & Regulatory | ⚖️ | Risques juridiques | Regulatory database |
| 9 | Cap Table Auditor | 📈 | Audit cap table | Terms database, preferences |
| 10 | GTM Analyst | 🚀 | Go-to-market | Channel benchmarks |
| 11 | Customer Intel | 🎯 | Analyse clients | Retention benchmarks |
| 12 | Exit Strategist | 🚪 | Scenarios de sortie | Exit database, acquirers |
| 13 | Question Master | ❓ | Questions killer | Pattern from failures |

### TIER 2 : Synthesis Agents (5 agents, sequential after Tier 1)

| # | Agent | Emoji | Mission | Input |
|---|-------|-------|---------|-------|
| 14 | Contradiction Detector | 🔀 | Trouve les incoherences | All Tier 1 outputs |
| 15 | Scenario Modeler | 📉 | Bull/Base/Bear | Financial + Market |
| 16 | Deal Scorer | 🎯 | Score final | All outputs |
| 17 | Devil's Advocate | 😈 | Challenge la these | All outputs |
| 18 | Memo Generator | 📝 | Documents finaux | All outputs |

### TIER 3 : Sector Experts (9 agents, activated by sector)

| # | Agent | Emoji | Activated When |
|---|-------|-------|----------------|
| 19 | SaaS Expert | 💻 | sector in ['SaaS', 'B2B Software'] |
| 20 | Marketplace Expert | 🏪 | sector in ['Marketplace', 'Platform'] |
| 21 | Fintech Expert | 🏦 | sector in ['Fintech', 'Payments', 'Banking'] |
| 22 | HealthTech Expert | 🏥 | sector in ['HealthTech', 'MedTech', 'BioTech'] |
| 23 | DeepTech Expert | 🔬 | sector in ['DeepTech', 'AI/ML', 'Quantum'] |
| 24 | Climate Expert | 🌱 | sector in ['CleanTech', 'Climate', 'Energy'] |
| 25 | Hardware Expert | 🏭 | sector in ['Hardware', 'IoT', 'Robotics'] |
| 26 | Gaming Expert | 🎮 | sector in ['Gaming', 'Esports', 'Metaverse'] |
| 27 | Consumer Expert | 📱 | sector in ['Consumer', 'D2C', 'Social'] |

## 4.2 Agent Base Template

Chaque agent suit la meme structure, avec le contexte injecte :

```typescript
interface AgentTemplate {

  // Identity
  id: string;
  name: string;
  emoji: string;
  tier: 1 | 2 | 3;

  // Execution
  execution: {
    parallel: boolean;           // Tier 1 = true
    depends_on: string[];        // For Tier 2
    activation_condition?: string; // For Tier 3
  };

  // Model selection
  model_preferences: {
    // NOT hardcoded models, but task characteristics
    requires_reasoning: boolean;
    requires_precision: boolean;
    requires_creativity: boolean;
    requires_web_search: boolean;
    max_acceptable_cost: number;
  };

  // Context requirements
  context_requirements: {
    similar_deals: boolean;
    benchmarks: boolean;
    competitors: boolean;
    founder_background: boolean;
    market_data: boolean;
    failure_cases: boolean;
  };

  // Prompt template
  prompt_template: string;  // Includes {context} placeholder

  // Output schema
  output_schema: {
    score?: boolean;
    red_flags?: boolean;
    green_flags?: boolean;
    questions?: boolean;
    recommendation?: boolean;
    structured_data?: JSONSchema;
  };
}
```

## 4.3 Example Agent: Financial Auditor

```typescript
const FinancialAuditor: AgentTemplate = {
  id: 'financial_auditor',
  name: 'Financial Auditor',
  emoji: '💰',
  tier: 1,

  execution: {
    parallel: true,
    depends_on: []
  },

  model_preferences: {
    requires_reasoning: true,
    requires_precision: true,
    requires_creativity: false,
    requires_web_search: false,
    max_acceptable_cost: 0.50
  },

  context_requirements: {
    similar_deals: true,
    benchmarks: true,
    competitors: true,
    founder_background: false,
    market_data: true,
    failure_cases: true
  },

  prompt_template: `
Tu es un FINANCIAL AUDITOR expert en due diligence financiere.

{context}

## TA MISSION

Auditer les metriques financieres de ce deal en les comparant
systematiquement aux benchmarks et deals similaires fournis.

## DEAL A ANALYSER

{deal_info}

## DOCUMENTS FINANCIERS

{financial_documents}

## TON ANALYSE

### 1. METRIQUES CLES VS BENCHMARKS

Pour chaque metrique, compare au contexte fourni:

| Metrique | Ce deal | Median secteur | Percentile | Verdict |
|----------|---------|----------------|------------|---------|
| ARR Growth | [X]% | {context.benchmarks.arr_growth.median}% | [Calc] | [Verdict] |
| Net Retention | [X]% | {context.benchmarks.ndr.median}% | [Calc] | [Verdict] |
| CAC | [X]€ | {context.benchmarks.cac.median}€ | [Calc] | [Verdict] |
| LTV/CAC | [X] | {context.benchmarks.ltv_cac.median} | [Calc] | [Verdict] |
| Burn Multiple | [X] | {context.benchmarks.burn_multiple.median} | [Calc] | [Verdict] |

### 2. COMPARAISON AUX DEALS SIMILAIRES

Parmi les {context.similar_deals.count} deals similaires:

| Metrique | Ce deal | P25 deals | Median deals | P75 deals | Position |
|----------|---------|-----------|--------------|-----------|----------|
[...]

**Deals les plus comparables:**
{context.similar_deals.top_3.map(d =>
  - {d.company}: {d.arr} ARR, {d.growth}% growth → {d.current_status}
)}

### 3. VALORISATION VS MARCHE

- **Demandee**: {deal.valuation}€ ({deal.arr_multiple}x ARR)
- **Median deals similaires**: {context.similar_deals.stats.median_multiple}x
- **Range P25-P75**: {context.similar_deals.stats.p25_multiple}x - {context.similar_deals.stats.p75_multiple}x
- **Position**: Percentile {calc}

**Attention**: {context.similar_deals.stats.down_round_percent}% des deals a
{context.threshold}x+ ont fait un down round depuis.

### 4. RED FLAGS FINANCIERS

🔴 **Critiques**: [...]
🟠 **A clarifier**: [...]

### 5. SCORE

**Score financier**: [X]/100

**Justification basee sur le contexte**:
[Pourquoi ce score, en reference aux benchmarks et deals similaires]
`,

  output_schema: {
    score: true,
    red_flags: true,
    green_flags: true,
    structured_data: {
      type: 'object',
      properties: {
        metrics_audit: {
          type: 'array',
          items: {
            metric: 'string',
            deal_value: 'number',
            benchmark_median: 'number',
            percentile: 'number',
            verdict: 'string'
          }
        },
        valuation_assessment: {
          type: 'object',
          properties: {
            asked_multiple: 'number',
            fair_range: { type: 'array', items: 'number' },
            verdict: 'string'
          }
        }
      }
    }
  }
};
```

---

# PARTIE 5 : INVESTMENT COMMITTEE TOOLKIT

## 5.1 Pre-IC Workflow

```
J-7 ─────────────────────────────────────────────────────────────────────────

  DEAL PACKAGE AUTO-GENERATION

  ┌──────────────────────────────────────────────────────────────────────┐
  │  📊 Full DD Report (20-30 pages)                                      │
  │  ├── Executive Summary                                                │
  │  ├── Agent Analyses (27 agents)                                       │
  │  ├── Context Data (similar deals, benchmarks, competitors)            │
  │  ├── Claim Verification Matrix                                        │
  │  ├── Red Flags & Concerns                                             │
  │  └── Questions for Founders                                           │
  │                                                                        │
  │  📝 One-Pager (pour review rapide)                                    │
  │                                                                        │
  │  📈 Financial Model + Scenarios                                        │
  │  ├── Bull case                                                        │
  │  ├── Base case                                                        │
  │  ├── Bear case                                                        │
  │  └── Sensitivity analysis                                             │
  │                                                                        │
  │  🔍 Competitive Landscape Map                                          │
  │                                                                        │
  │  👥 Founder Background Report                                          │
  └──────────────────────────────────────────────────────────────────────┘

J-3 ─────────────────────────────────────────────────────────────────────────

  PRE-VOTE (Anonyme)

  ┌──────────────────────────────────────────────────────────────────────┐
  │  Chaque partner recoit le package et vote:                            │
  │                                                                        │
  │  Score (1-5): [  ]                                                    │
  │                                                                        │
  │  Top concerns:                                                        │
  │  1. ____________________                                              │
  │  2. ____________________                                              │
  │  3. ____________________                                              │
  │                                                                        │
  │  Questions I want discussed:                                          │
  │  1. ____________________                                              │
  │  2. ____________________                                              │
  │                                                                        │
  │  Initial leaning: [ ] Strong Invest  [ ] Invest  [ ] Pass            │
  └──────────────────────────────────────────────────────────────────────┘

J-1 ─────────────────────────────────────────────────────────────────────────

  DIVERGENCE ANALYSIS

  ┌──────────────────────────────────────────────────────────────────────┐
  │  Pre-vote results:                                                    │
  │                                                                        │
  │  Partner A: 4/5 - Invest      │ ┌─────────────────────────────────┐  │
  │  Partner B: 2/5 - Pass        │ │ DIVERGENCE DETECTED             │  │
  │  Partner C: 4/5 - Invest      │ │                                  │  │
  │  Partner D: 3/5 - Unsure      │ │ Partner B sees risks others     │  │
  │                                │ │ don't. Key concerns:            │  │
  │  Average: 3.25/5               │ │ - Founder track record          │  │
  │  Std Dev: 0.96 (HIGH)          │ │ - Market timing                 │  │
  │                                │ │                                  │  │
  │                                │ │ → Prioritize these in meeting   │  │
  │                                │ └─────────────────────────────────┘  │
  └──────────────────────────────────────────────────────────────────────┘
```

## 5.2 IC Meeting Dashboard

```typescript
interface ICMeetingDashboard {

  // Meeting structure
  structure: {
    sections: [
      { name: 'Deal Overview', duration: 10, presenter: 'lead_partner' },
      { name: 'Key Findings', duration: 15, presenter: 'analyst' },
      { name: 'Concerns Discussion', duration: 20, presenter: 'all' },
      { name: 'Devil\'s Advocate', duration: 10, presenter: 'designated' },
      { name: 'Final Discussion', duration: 10, presenter: 'all' },
      { name: 'Vote', duration: 5, presenter: 'chair' }
    ];
    total_duration: 70;
  };

  // Live features
  liveFeatures: {
    // Timer
    timer: {
      current_section: string;
      time_remaining: number;
      alerts: ['5_min_warning', '1_min_warning', 'overtime'];
    };

    // Collaborative notes
    notes: {
      real_time: true;
      categorized: ['observation', 'concern', 'question', 'action_item'];
      attributed: true;  // Who wrote what
    };

    // Question tracking
    questions: {
      from_pre_vote: Question[];
      raised_in_meeting: Question[];
      status: 'pending' | 'discussed' | 'answered' | 'deferred';
    };

    // Sentiment tracking
    sentiment: {
      real_time_pulse: boolean;
      anonymous_reactions: ['agree', 'disagree', 'concern', 'question'];
    };

    // Live voting
    voting: {
      type: 'invest' | 'pass' | 'need_more_info' | 'conditional';
      with_conditions: boolean;
      with_conviction: 'high' | 'medium' | 'low';
      anonymous_until_all_voted: true;
    };
  };
}
```

## 5.3 Post-IC Workflow

```
IMMEDIATE (J+0)
─────────────────────────────────────────────────────────────────────────────

  ┌──────────────────────────────────────────────────────────────────────┐
  │  DECISION RECORD                                                      │
  │                                                                        │
  │  Decision: [INVEST / PASS / CONDITIONAL]                              │
  │                                                                        │
  │  Vote breakdown:                                                      │
  │  - Strong Invest: 2                                                   │
  │  - Invest: 1                                                          │
  │  - Pass: 1                                                            │
  │  - Conditional: 0                                                     │
  │                                                                        │
  │  Key reasons:                                                         │
  │  1. [Auto-captured from discussion]                                   │
  │  2. [Auto-captured from discussion]                                   │
  │  3. [Auto-captured from discussion]                                   │
  │                                                                        │
  │  Dissenting view (Partner B):                                         │
  │  [Captured for learning]                                              │
  │                                                                        │
  │  Conditions (if conditional):                                         │
  │  1. [Condition]                                                       │
  │  2. [Condition]                                                       │
  └──────────────────────────────────────────────────────────────────────┘

IF INVEST ───────────────────────────────────────────────────────────────────

  Auto-triggered:

  [ ] Term sheet checklist activated
  [ ] Legal DD items flagged for review
  [ ] Reference check emails generated
  [ ] Data room access request prepared
  [ ] Board observation/seat prep initiated
  [ ] Investment memo for file created

IF PASS ─────────────────────────────────────────────────────────────────────

  Auto-generated:

  ┌──────────────────────────────────────────────────────────────────────┐
  │  FOUNDER FEEDBACK EMAIL (Draft)                                       │
  │                                                                        │
  │  Subject: [Company] - Our Decision                                    │
  │                                                                        │
  │  Hi [Founder],                                                        │
  │                                                                        │
  │  Thank you for sharing [Company] with us. After careful               │
  │  consideration, we've decided not to proceed at this time.            │
  │                                                                        │
  │  Key factors in our decision:                                         │
  │  - [Reason 1 - honest but kind]                                       │
  │  - [Reason 2 - honest but kind]                                       │
  │                                                                        │
  │  What would make us reconsider:                                       │
  │  - [Milestone 1]                                                      │
  │  - [Milestone 2]                                                      │
  │                                                                        │
  │  We'd love to stay in touch and revisit in [timeframe].               │
  │                                                                        │
  │  Best,                                                                │
  │  [Partner]                                                            │
  └──────────────────────────────────────────────────────────────────────┘

  Also:
  [ ] Deal tagged for follow-up in [X] months
  [ ] Lessons learned captured in system
  [ ] Competitor tracking maintained

ONGOING ─────────────────────────────────────────────────────────────────────

  Tracking & Learning:

  [ ] Decision logged with predictions
  [ ] Outcome tracking scheduled
  [ ] Quarterly review calendar entry
  [ ] Competitor alerts active
```

---

# PARTIE 6 : DATABASE SCHEMA

```sql
-- ═══════════════════════════════════════════════════════════════════════════
-- FULLINVEST v4.0 - COMPLETE SCHEMA
-- ═══════════════════════════════════════════════════════════════════════════

-- Extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";  -- For text search

-- ───────────────────────────────────────────────────────────────────────────
-- CORE TABLES
-- ───────────────────────────────────────────────────────────────────────────

-- Deals
CREATE TABLE public.deals (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,

  -- Basic info
  name TEXT NOT NULL,
  company_name TEXT,
  website TEXT,
  description TEXT,

  -- Classification
  sector TEXT,
  sub_sector TEXT,
  stage TEXT,
  business_model TEXT,
  geography TEXT,

  -- Financials
  arr DECIMAL(12,2),
  mrr DECIMAL(12,2),
  growth_rate DECIMAL(5,2),
  amount_requested DECIMAL(12,2),
  valuation_pre DECIMAL(14,2),
  valuation_post DECIMAL(14,2),

  -- Status
  status TEXT DEFAULT 'screening',
  screening_score INTEGER,
  full_dd_score INTEGER,
  final_score INTEGER,
  recommendation TEXT,
  conviction TEXT,

  -- IC
  ic_scheduled_date TIMESTAMPTZ,
  ic_decision TEXT,
  ic_decision_date TIMESTAMPTZ,
  ic_conditions TEXT[],

  -- Context quality
  context_completeness DECIMAL(3,2),
  context_freshness TIMESTAMPTZ,

  -- Encrypted fields
  source_intro_encrypted TEXT,
  notes_encrypted TEXT,

  -- Timestamps
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),

  -- Metadata
  metadata JSONB DEFAULT '{}'
);

-- ───────────────────────────────────────────────────────────────────────────
-- CONTEXT TABLES
-- ───────────────────────────────────────────────────────────────────────────

-- Similar deals (cached from external APIs)
CREATE TABLE public.similar_deals (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,

  -- External company info
  external_company_name TEXT NOT NULL,
  external_company_id TEXT,  -- Crunchbase/Dealroom ID
  source TEXT NOT NULL,      -- 'crunchbase', 'dealroom', etc.

  -- Match info
  similarity_score DECIMAL(3,2),
  matching_factors TEXT[],

  -- Company data
  sector TEXT,
  stage TEXT,
  geography TEXT,

  -- Funding data
  raised_amount DECIMAL(14,2),
  valuation DECIMAL(14,2),
  arr_at_raise DECIMAL(12,2),
  arr_multiple DECIMAL(5,2),
  funding_date DATE,
  investors TEXT[],
  lead_investor TEXT,

  -- Current status
  current_status TEXT,  -- 'thriving', 'growing', 'flat', 'struggling', 'dead', 'exited'
  current_arr DECIMAL(12,2),
  last_status_update TIMESTAMPTZ,

  -- Exit info
  exit_type TEXT,
  exit_value DECIMAL(14,2),
  exit_multiple DECIMAL(5,2),
  exit_date DATE,
  acquirer TEXT,

  -- Cache management
  fetched_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ
);

CREATE INDEX idx_similar_deals_deal ON similar_deals(deal_id);
CREATE INDEX idx_similar_deals_sector ON similar_deals(sector);

-- Market benchmarks (cached)
CREATE TABLE public.market_benchmarks (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),

  -- Scope
  sector TEXT NOT NULL,
  stage TEXT NOT NULL,
  geography TEXT DEFAULT 'global',
  source TEXT NOT NULL,

  -- Metrics (JSONB for flexibility)
  metrics JSONB NOT NULL,
  /*
  {
    "arr_growth": { "p25": 50, "median": 80, "p75": 120, "p90": 200 },
    "net_retention": { "p25": 95, "median": 110, "p75": 130 },
    ...
  }
  */

  -- Validity
  valid_from DATE,
  valid_to DATE,
  fetched_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_benchmarks_lookup ON market_benchmarks(sector, stage);

-- Competitors
CREATE TABLE public.competitors (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),

  -- Identity
  name TEXT NOT NULL,
  website TEXT,
  description TEXT,

  -- Classification
  sector TEXT,
  sub_sector TEXT,

  -- Company info
  founded_year INTEGER,
  hq_location TEXT,
  employee_count INTEGER,
  employee_growth_6m DECIMAL(5,2),

  -- Funding
  total_raised DECIMAL(14,2),
  last_round_amount DECIMAL(14,2),
  last_round_date DATE,
  last_round_type TEXT,
  last_round_valuation DECIMAL(14,2),
  investors TEXT[],

  -- External IDs
  crunchbase_id TEXT,
  linkedin_id TEXT,
  dealroom_id TEXT,

  -- Traction signals
  web_traffic_monthly INTEGER,
  web_traffic_trend TEXT,
  social_following INTEGER,

  -- Cache
  last_updated TIMESTAMPTZ DEFAULT NOW()
);

CREATE UNIQUE INDEX idx_competitors_name ON competitors(name);

-- Deal-Competitor relationship
CREATE TABLE public.deal_competitors (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,
  competitor_id UUID NOT NULL REFERENCES competitors(id),

  -- Relationship
  relationship_type TEXT,  -- 'direct', 'indirect', 'potential', 'big_tech'
  threat_level TEXT,       -- 'critical', 'high', 'medium', 'low'
  mentioned_in_deck BOOLEAN DEFAULT FALSE,
  discovered_by TEXT,      -- Agent that found it

  -- Analysis
  strengths TEXT[],
  weaknesses TEXT[],
  vs_deal_company JSONB,

  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Founder background (cached)
CREATE TABLE public.founder_backgrounds (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,

  -- Identity
  founder_name TEXT NOT NULL,
  founder_role TEXT,

  -- LinkedIn
  linkedin_url TEXT,
  linkedin_data JSONB,

  -- Previous ventures
  previous_ventures JSONB,
  /*
  [
    {
      "company": "...",
      "role": "...",
      "dates": {...},
      "outcome": "...",
      "funding": {...},
      "sources": [...]
    }
  ]
  */

  -- Background verification
  claimed_background JSONB,
  verified_background JSONB,
  discrepancies JSONB,

  -- Red flags search
  red_flags JSONB,
  /*
  {
    "legal": [...],
    "professional": [...],
    "found_issues": [...]
  }
  */

  -- Network
  network_analysis JSONB,

  -- Cache
  last_updated TIMESTAMPTZ DEFAULT NOW()
);

-- ───────────────────────────────────────────────────────────────────────────
-- DOCUMENT TABLES
-- ───────────────────────────────────────────────────────────────────────────

-- Documents
CREATE TABLE public.deal_documents (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,

  -- File info
  name TEXT NOT NULL,
  type TEXT NOT NULL,
  storage_path TEXT NOT NULL,
  mime_type TEXT,
  size_bytes INTEGER,

  -- Processing
  processing_status TEXT DEFAULT 'pending',
  processed_at TIMESTAMPTZ,

  -- Extracted content
  extracted_text TEXT,
  extracted_structure JSONB,  -- For slides, tables, etc.

  uploaded_at TIMESTAMPTZ DEFAULT NOW()
);

-- Claims
CREATE TABLE public.deal_claims (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,
  document_id UUID REFERENCES deal_documents(id),

  -- Claim
  claim_text TEXT NOT NULL,
  claim_category TEXT,
  claim_type TEXT,
  source_location TEXT,
  extracted_value TEXT,

  -- Verification
  verification_status TEXT DEFAULT 'pending',
  verification_source TEXT,
  verification_url TEXT,
  verified_by_agent TEXT,
  confidence_score DECIMAL(3,2),

  -- Context comparison
  context_comparison JSONB,
  /*
  {
    "similar_deals_say": "...",
    "benchmarks_show": "...",
    "discrepancy": "..."
  }
  */

  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_claims_deal ON deal_claims(deal_id);
CREATE INDEX idx_claims_status ON deal_claims(verification_status);

-- ───────────────────────────────────────────────────────────────────────────
-- AGENT & MODEL TABLES
-- ───────────────────────────────────────────────────────────────────────────

-- Agent outputs
CREATE TABLE public.agent_outputs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,

  -- Agent
  agent_id TEXT NOT NULL,
  agent_tier INTEGER NOT NULL,

  -- Model used
  model_id TEXT NOT NULL,
  model_selection_reason TEXT,

  -- Output
  content TEXT NOT NULL,
  structured_output JSONB,

  -- Scoring
  score INTEGER,
  confidence TEXT,
  red_flags TEXT[],
  green_flags TEXT[],

  -- Context used
  context_summary TEXT,
  context_version TEXT,

  -- Sources
  sources JSONB,

  -- Execution
  tokens_input INTEGER,
  tokens_output INTEGER,
  cost_usd DECIMAL(6,4),
  execution_time_ms INTEGER,

  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_agent_outputs_deal ON agent_outputs(deal_id);
CREATE INDEX idx_agent_outputs_agent ON agent_outputs(agent_id);

-- Model performance tracking
CREATE TABLE public.model_performance (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),

  -- Model
  model_id TEXT NOT NULL,
  task_type TEXT NOT NULL,

  -- Aggregated metrics
  sample_size INTEGER DEFAULT 0,
  avg_quality_score DECIMAL(5,2),
  avg_latency_ms INTEGER,
  success_rate DECIMAL(3,2),
  cost_efficiency DECIMAL(5,2),

  -- Trend
  quality_trend TEXT,  -- 'improving', 'stable', 'declining'

  -- Period
  period_start DATE,
  period_end DATE,

  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE UNIQUE INDEX idx_model_perf_lookup ON model_performance(model_id, task_type, period_start);

-- Model diversity matrix
CREATE TABLE public.model_diversity (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),

  model_a TEXT NOT NULL,
  model_b TEXT NOT NULL,
  task_type TEXT NOT NULL,

  -- Disagreement rate
  sample_size INTEGER DEFAULT 0,
  disagreement_rate DECIMAL(3,2),  -- How often they disagree

  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE UNIQUE INDEX idx_diversity_lookup ON model_diversity(model_a, model_b, task_type);

-- Ensemble outputs
CREATE TABLE public.ensemble_outputs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,

  -- Question
  question_type TEXT NOT NULL,
  question TEXT NOT NULL,

  -- Individual responses
  individual_responses JSONB NOT NULL,
  /*
  [
    { "model": "...", "response": "...", "key_points": [...] },
    ...
  ]
  */

  -- Analysis
  consensus JSONB,
  disagreements JSONB,
  unique_insights JSONB,

  -- Fusion
  fusion_model TEXT,
  synthesized_response TEXT,
  final_recommendation TEXT,
  confidence TEXT,

  -- Open questions
  open_questions TEXT[],

  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- ───────────────────────────────────────────────────────────────────────────
-- IC TABLES
-- ───────────────────────────────────────────────────────────────────────────

-- IC Meetings
CREATE TABLE public.ic_meetings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,

  -- Scheduling
  scheduled_date TIMESTAMPTZ,
  actual_date TIMESTAMPTZ,
  duration_minutes INTEGER,
  status TEXT DEFAULT 'scheduled',

  -- Pre-vote
  pre_votes JSONB,
  pre_vote_divergence DECIMAL(3,2),

  -- Meeting
  attendees UUID[],
  notes JSONB,
  questions_discussed JSONB,

  -- Decision
  final_votes JSONB,
  decision TEXT,
  decision_rationale TEXT,
  conditions TEXT[],
  dissenting_views JSONB,

  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- ───────────────────────────────────────────────────────────────────────────
-- LEARNING TABLES
-- ───────────────────────────────────────────────────────────────────────────

-- Quality feedback
CREATE TABLE public.quality_feedback (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),

  -- Reference
  agent_output_id UUID REFERENCES agent_outputs(id),
  model_id TEXT NOT NULL,
  task_type TEXT NOT NULL,

  -- Feedback
  quality_score INTEGER,  -- 1-100
  accuracy_score INTEGER,
  helpfulness_score INTEGER,

  -- Source
  feedback_source TEXT,  -- 'user', 'automated', 'outcome'

  -- Details
  comments TEXT,
  corrections TEXT,

  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Deal outcomes (for learning)
CREATE TABLE public.deal_outcomes (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  deal_id UUID NOT NULL REFERENCES deals(id) ON DELETE CASCADE,

  -- What we predicted
  predictions JSONB NOT NULL,
  /*
  {
    "score": 75,
    "recommendation": "invest",
    "confidence": "high",
    "key_risks": [...],
    "expected_multiple": 5
  }
  */
  prediction_date TIMESTAMPTZ,

  -- Investment (if made)
  invested BOOLEAN,
  invested_amount DECIMAL(12,2),
  invested_date DATE,
  ownership_percentage DECIMAL(5,2),

  -- Current status
  current_status TEXT,
  current_valuation DECIMAL(14,2),
  current_multiple DECIMAL(5,2),

  -- Exit (if exited)
  exit_date DATE,
  exit_type TEXT,
  exit_amount DECIMAL(14,2),
  exit_multiple DECIMAL(5,2),

  -- Learnings
  surprises TEXT[],
  lessons_learned TEXT[],
  what_we_got_right TEXT[];
  what_we_got_wrong TEXT[],

  last_updated TIMESTAMPTZ DEFAULT NOW()
);

-- ───────────────────────────────────────────────────────────────────────────
-- ROW LEVEL SECURITY
-- ───────────────────────────────────────────────────────────────────────────

ALTER TABLE public.deals ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.deal_documents ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.deal_claims ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.agent_outputs ENABLE ROW LEVEL SECURITY;
-- ... (Apply to all user-scoped tables)

CREATE POLICY "Users can manage own deals" ON public.deals
  FOR ALL USING (auth.uid() = user_id);

-- ... (Similar policies for related tables)
```

---

# PARTIE 7 : IMPLEMENTATION ROADMAP

## Phase 1: Foundation (Weeks 1-4)

```
WEEK 1-2: Core Infrastructure
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[ ] Next.js 14 + Supabase setup
[ ] OpenRouter integration with basic routing
[ ] Model registry and health checks
[ ] Basic auth and deal CRUD
[ ] Document upload and storage

WEEK 3-4: Context Engine MVP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[ ] Similar deals fetching (Crunchbase Basic)
[ ] Basic benchmarks (public data)
[ ] Context injection into prompts
[ ] Web search integration (Tavily)
```

## Phase 2: Agents (Weeks 5-8)

```
WEEK 5-6: Tier 1 Agents
━━━━━━━━━━━━━━━━━━━━━━━
[ ] Implement 13 Tier 1 agents
[ ] Parallel execution engine
[ ] Context-aware prompts
[ ] Output parsing and structuring

WEEK 7-8: Tier 2 & 3 Agents
━━━━━━━━━━━━━━━━━━━━━━━━━━━
[x] Implement 5 Tier 2 synthesis agents
[x] Implement 9 Tier 3 sector experts
[x] Agent orchestration
[x] Basic scoring system
```

## Phase 3: Multi-Model (Weeks 9-12)

```
WEEK 9-10: Model Router
━━━━━━━━━━━━━━━━━━━━━━━
[ ] Performance tracking per model/task
[ ] Routing strategies implementation
[ ] Fallback handling
[ ] Exploration mechanism

WEEK 11-12: Ensemble Layer
━━━━━━━━━━━━━━━━━━━━━━━━━
[ ] Multi-model critical questions
[ ] Response analysis
[ ] Fusion engine
[ ] Diversity learning
```

## Phase 4: IC Toolkit (Weeks 13-16)

```
WEEK 13-14: Pre-IC
━━━━━━━━━━━━━━━━━━
[ ] Package generation
[ ] Pre-vote system
[ ] Divergence analysis

WEEK 15-16: Meeting & Post-IC
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[ ] Meeting dashboard
[ ] Decision capture
[ ] Post-IC workflows
[ ] Outcome tracking
```

## Phase 5: Learning (Weeks 17-20)

```
WEEK 17-18: Performance Learning
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[ ] Quality feedback collection
[ ] Model performance aggregation
[ ] Automatic routing optimization

WEEK 19-20: Calibration & Polish
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[ ] Prediction calibration
[ ] Outcome analysis
[ ] System refinements
```

---

# PARTIE 8 : SUCCESS METRICS

## Product Metrics

| Metric | MVP | v1 | v2 |
|--------|-----|----|----|
| Time to Full DD | <20 min | <10 min | <5 min |
| Context coverage | 50% | 80% | 95% |
| Claim verification rate | 60% | 80% | 95% |
| Model routing accuracy | N/A | 70% | 85% |
| Ensemble value-add | N/A | Measured | +20% quality |
| Prediction calibration | N/A | Measured | <10% error |

## Business Metrics

| Metric | Year 1 |
|--------|--------|
| Paying customers | 100 |
| ARR | 500K€ |
| NRR | >120% |
| Deals analyzed | 10,000+ |

---

# ANNEXES

## A. Environment Variables

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OpenRouter (Model Gateway)
OPENROUTER_API_KEY=

# Context Data Sources
CRUNCHBASE_API_KEY=
DEALROOM_API_KEY=
PITCHBOOK_API_KEY=    # If available

# Web Search
TAVILY_API_KEY=

# Encryption
ENCRYPTION_KEY=

# Optional
NOTION_API_KEY=
SLACK_WEBHOOK_URL=
```

## B. Cost Estimates per Deal

| Component | Est. Cost |
|-----------|-----------|
| Context fetching (APIs) | $0.50-2.00 |
| Tier 1 agents (13x) | $1.50-3.00 |
| Tier 2 agents (5x) | $0.75-1.50 |
| Tier 3 agent (1x) | $0.25-0.50 |
| Ensemble (3 critical Q) | $1.00-2.00 |
| **Total** | **$4-9** |

Note: Costs vary by model mix and context depth.

---

**Version**: 4.0.0 - Context-Aware, Model-Agnostic

**Date**: 2026-01-18

**Status**: Ready for implementation
